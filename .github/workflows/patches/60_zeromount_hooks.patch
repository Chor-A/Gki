diff --git a/fs/Kconfig b/fs/Kconfig
--- a/fs/Kconfig
+++ b/fs/Kconfig
@@ -350,6 +350,13 @@ source "fs/unicode/Kconfig"
 config IO_WQ
 	bool
 
+config ZEROMOUNT
+	bool "ZeroMount Path Redirection Subsystem"
+	default y
+	help
+	  ZeroMount allows path redirection and virtual file injection
+	  without mounting filesystems. Useful for systemless modifications.
+
 endmenu
 
 config KSU_SUSFS_SUS_KSTAT_REDIRECT
diff --git a/fs/Makefile b/fs/Makefile
--- a/fs/Makefile
+++ b/fs/Makefile
@@ -139,3 +139,4 @@ obj-$(CONFIG_EFIVAR_FS)		+= efivarfs/
 obj-$(CONFIG_EROFS_FS)		+= erofs/
 obj-$(CONFIG_VBOXSF_FS)		+= vboxsf/
 obj-$(CONFIG_ZONEFS_FS)		+= zonefs/
+obj-$(CONFIG_ZEROMOUNT)		+= zeromount.o
diff --git a/fs/d_path.c b/fs/d_path.c
--- a/fs/d_path.c
+++ b/fs/d_path.c
@@ -8,6 +8,10 @@
 #include <linux/prefetch.h>
 #include "mount.h"
 
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
+
 static int prepend(char **buffer, int *buflen, const char *str, int namelen)
 {
 	*buflen -= namelen;
@@ -265,6 +269,24 @@ char *d_path(const struct path *path, char *buf, int buflen)
 	if (path->dentry->d_op && path->dentry->d_op->d_dname &&
 	    (!IS_ROOT(path->dentry) || path->dentry != path->mnt->mnt_root))
 		return path->dentry->d_op->d_dname(path->dentry, buf, buflen);
+
+#ifdef CONFIG_ZEROMOUNT
+	if (path->dentry && d_backing_inode(path->dentry)) {
+		char *v_path = zeromount_get_static_vpath(d_backing_inode(path->dentry));
+		if (v_path) {
+			int len = strlen(v_path);
+			if (buflen < len + 1) {
+				kfree(v_path);
+				return ERR_PTR(-ENAMETOOLONG);
+			}
+			*--res = '\0';
+			res -= len;
+			memcpy(res, v_path, len);
+			kfree(v_path);
+			return res;
+		}
+	}
+#endif
 
 	/*
 	 * We have various synthetic filesystems that never get mounted.  On
diff --git a/fs/namei.c b/fs/namei.c
--- a/fs/namei.c
+++ b/fs/namei.c
@@ -43,6 +43,9 @@
 #ifdef CONFIG_KSU_SUSFS
 #include <linux/susfs.h>
 #endif
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
 #if defined(CONFIG_KSU_SUSFS_SUS_PATH) || defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)
 #include <linux/susfs_def.h>
 #endif
@@ -210,6 +213,11 @@ getname_flags(const char __user *filename, int flags, int *empty)
 		result->refcnt = 1;
 	}
 
+#ifdef CONFIG_ZEROMOUNT
+	if (!IS_ERR(result)) {
+		result = zeromount_getname_hook(result);
+	}
+#endif
 	audit_getname(result);
 	return result;
 }
@@ -361,6 +369,16 @@ int generic_permission(struct inode *inode, int mask)
 {
 	int ret;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (zeromount_is_injected_file(inode)) {
+		if (mask & MAY_WRITE)
+			return -EACCES;
+		return 0;
+	}
+	if (S_ISDIR(inode->i_mode) && zeromount_is_traversal_allowed(inode, mask)) {
+		return 0;
+	}
+#endif
 	/*
 	 * Do the basic permission checks.
 	 */
@@ -454,6 +472,16 @@ int inode_permission(struct inode *inode, int mask)
 {
 	int retval;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (zeromount_is_injected_file(inode)) {
+		if (mask & MAY_WRITE)
+			return -EACCES;
+		return 0;
+	}
+	if (S_ISDIR(inode->i_mode) && zeromount_is_traversal_allowed(inode, mask)) {
+		return 0;
+	}
+#endif
 	retval = sb_permission(inode->i_sb, inode, mask);
 	if (retval)
 		return retval;
diff --git a/fs/proc/base.c b/fs/proc/base.c
--- a/fs/proc/base.c
+++ b/fs/proc/base.c
@@ -102,6 +102,9 @@
 #if defined(CONFIG_KSU_SUSFS_SUS_MAP) || defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)
 #include <linux/susfs_def.h>
 #endif // #if defined(CONFIG_KSU_SUSFS_SUS_MAP) || defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
 
 #include <trace/events/oom.h>
 #include "internal.h"
@@ -1835,6 +1838,25 @@ static int do_proc_readlink(struct path *path, char __user *buffer, int buflen)
 	if (!tmp)
 		return -ENOMEM;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (!zeromount_should_skip() && path->dentry) {
+		struct inode *inode = d_backing_inode(path->dentry);
+		if (inode) {
+			char *vpath = zeromount_get_static_vpath(inode);
+			if (vpath) {
+				int vlen = strlen(vpath);
+				if (vlen > buflen)
+					vlen = buflen;
+				if (copy_to_user(buffer, vpath, vlen) == 0) {
+					kfree(vpath);
+					free_page((unsigned long)tmp);
+					return vlen;
+				}
+				kfree(vpath);
+			}
+		}
+	}
+#endif
 
 #ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
 	if (SUSFS_IS_INODE_OPEN_REDIRECT(path->dentry->d_inode)) {
diff --git a/fs/proc/task_mmu.c b/fs/proc/task_mmu.c
--- a/fs/proc/task_mmu.c
+++ b/fs/proc/task_mmu.c
@@ -23,6 +23,9 @@
 #if defined(CONFIG_KSU_SUSFS_SUS_KSTAT) || defined(CONFIG_KSU_SUSFS_SUS_MAP) || defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)
 #include <linux/susfs_def.h>
 #endif // #if defined(CONFIG_KSU_SUSFS_SUS_KSTAT) || defined(CONFIG_KSU_SUSFS_SUS_MAP) || defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
 
 #include <asm/elf.h>
 #include <asm/tlb.h>
@@ -369,6 +372,9 @@ show_map_vma(struct seq_file *m, struct vm_area_struct *vma)
 		dev = inode->i_sb->s_dev;
 		ino = inode->i_ino;
 		pgoff = ((loff_t)vma->vm_pgoff) << PAGE_SHIFT;
+#ifdef CONFIG_ZEROMOUNT
+		zeromount_spoof_mmap_metadata(inode, &dev, &ino);
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_KSTAT
 		if (inode->i_mapping &&
 			unlikely(test_bit(AS_FLAGS_SUS_KSTAT, &inode->i_mapping->flags) &&
diff --git a/fs/readdir.c b/fs/readdir.c
--- a/fs/readdir.c
+++ b/fs/readdir.c
@@ -28,6 +28,9 @@
 extern int susfs_get_data_path(struct path *path);
 extern bool susfs_is_inode_sus_path(struct inode *inode);
 #endif
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
 /*
  * Note the "unsafe_put_user() semantics: we goto a
  * label for errors.
@@ -190,6 +193,9 @@ SYSCALL_DEFINE3(old_readdir, unsigned int, fd,
 		.ctx.actor = fillonedir,
 		.dirent = dirent
 	};
+#ifdef CONFIG_ZEROMOUNT
+	int initial_count = count;
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	int path_err = -EINVAL;
 	struct path path;
@@ -199,6 +205,13 @@ SYSCALL_DEFINE3(old_readdir, unsigned int, fd,
 	if (!f.file)
 		return -EBADF;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (f.file->f_pos >= ZEROMOUNT_MAGIC_POS) {
+		error = 0;
+		goto skip_real_iterate;
+	}
+#endif
+
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (f.file->f_inode->i_sb->s_magic == FUSE_SUPER_MAGIC) {
 		path_err = susfs_get_data_path(&path);
@@ -215,6 +228,16 @@ orig_flow:
 	if (buf.result)
 		error = buf.result;
 
+#ifdef CONFIG_ZEROMOUNT
+skip_real_iterate:
+	if (error >= 0 && !signal_pending(current)) {
+		zeromount_inject_dents(f.file, (void __user **)&dirent, &count, &f.file->f_pos);
+		if (count != initial_count)
+			error = initial_count - count;
+		goto zm_out;
+	}
+zm_out:
+#endif
 	fdput_pos(f);
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (!path_err)
@@ -323,6 +346,9 @@ SYSCALL_DEFINE3(getdents, unsigned int, fd,
 		.current_dir = dirent
 	};
 	int error;
+#ifdef CONFIG_ZEROMOUNT
+	int initial_count = count;
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	int path_err = -EINVAL;
 	struct path path;
@@ -332,6 +358,13 @@ SYSCALL_DEFINE3(getdents, unsigned int, fd,
 	if (!f.file)
 		return -EBADF;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (f.file->f_pos >= ZEROMOUNT_MAGIC_POS) {
+		error = 0;
+		goto skip_real_iterate;
+	}
+#endif
+
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (f.file->f_inode->i_sb->s_magic == FUSE_SUPER_MAGIC) {
 		path_err = susfs_get_data_path(&path);
@@ -355,6 +388,16 @@ orig_flow:
 		else
 			error = count - buf.count;
 	}
+#ifdef CONFIG_ZEROMOUNT
+skip_real_iterate:
+	if (error >= 0 && !signal_pending(current)) {
+		zeromount_inject_dents(f.file, (void __user **)&dirent, &count, &f.file->f_pos);
+		if (count != initial_count)
+			error = initial_count - count;
+		goto zm_out;
+	}
+zm_out:
+#endif
 	fdput_pos(f);
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (!path_err)
@@ -444,6 +487,9 @@ SYSCALL_DEFINE3(getdents64, unsigned int, fd,
 		.current_dir = dirent
 	};
 	int error;
+#ifdef CONFIG_ZEROMOUNT
+	int initial_count = count;
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	int path_err = -EINVAL;
 	struct path path;
@@ -453,6 +499,13 @@ SYSCALL_DEFINE3(getdents64, unsigned int, fd,
 	if (!f.file)
 		return -EBADF;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (f.file->f_pos >= ZEROMOUNT_MAGIC_POS) {
+		error = 0;
+		goto skip_real_iterate;
+	}
+#endif
+
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (f.file->f_inode->i_sb->s_magic == FUSE_SUPER_MAGIC) {
 		path_err = susfs_get_data_path(&path);
@@ -477,6 +530,16 @@ orig_flow:
 		else
 			error = count - buf.count;
 	}
+#ifdef CONFIG_ZEROMOUNT
+skip_real_iterate:
+	if (error >= 0 && !signal_pending(current)) {
+		zeromount_inject_dents64(f.file, (void __user **)&dirent, &count, &f.file->f_pos);
+		if (count != initial_count)
+			error = initial_count - count;
+		goto zm_out;
+	}
+zm_out:
+#endif
 	fdput_pos(f);
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (!path_err)
@@ -565,6 +628,9 @@ COMPAT_SYSCALL_DEFINE3(old_readdir, unsigned int, fd,
 		.ctx.actor = compat_fillonedir,
 		.dirent = dirent
 	};
+#ifdef CONFIG_ZEROMOUNT
+	int initial_count = count;
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	int path_err = -EINVAL;
 	struct path path;
@@ -574,6 +640,13 @@ COMPAT_SYSCALL_DEFINE3(old_readdir, unsigned int, fd,
 	if (!f.file)
 		return -EBADF;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (f.file->f_pos >= ZEROMOUNT_MAGIC_POS) {
+		error = 0;
+		goto skip_real_iterate;
+	}
+#endif
+
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (f.file->f_inode->i_sb->s_magic == FUSE_SUPER_MAGIC) {
 		path_err = susfs_get_data_path(&path);
@@ -588,6 +661,16 @@ orig_flow:
 	if (buf.result)
 		error = buf.result;
 
+#ifdef CONFIG_ZEROMOUNT
+skip_real_iterate:
+	if (error >= 0 && !signal_pending(current)) {
+		zeromount_inject_dents(f.file, (void __user **)&dirent, &count, &f.file->f_pos);
+		if (count != initial_count)
+			error = initial_count - count;
+		goto zm_out;
+	}
+zm_out:
+#endif
 	fdput_pos(f);
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (!path_err)
@@ -688,6 +771,9 @@ COMPAT_SYSCALL_DEFINE3(getdents, unsigned int, fd,
 		.count = count
 	};
 	int error;
+#ifdef CONFIG_ZEROMOUNT
+	int initial_count = count;
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	int path_err = -EINVAL;
 	struct path path;
@@ -697,6 +783,13 @@ COMPAT_SYSCALL_DEFINE3(getdents, unsigned int, fd,
 	if (!f.file)
 		return -EBADF;
 
+#ifdef CONFIG_ZEROMOUNT
+	if (f.file->f_pos >= ZEROMOUNT_MAGIC_POS) {
+		error = 0;
+		goto skip_real_iterate;
+	}
+#endif
+
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (f.file->f_inode->i_sb->s_magic == FUSE_SUPER_MAGIC) {
 		path_err = susfs_get_data_path(&path);
@@ -720,6 +813,16 @@ orig_flow:
 		else
 			error = count - buf.count;
 	}
+#ifdef CONFIG_ZEROMOUNT
+skip_real_iterate:
+	if (error >= 0 && !signal_pending(current)) {
+		zeromount_inject_dents(f.file, (void __user **)&dirent, &count, &f.file->f_pos);
+		if (count != initial_count)
+			error = initial_count - count;
+		goto zm_out;
+	}
+zm_out:
+#endif
 	fdput_pos(f);
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 	if (!path_err)
diff --git a/fs/stat.c b/fs/stat.c
--- a/fs/stat.c
+++ b/fs/stat.c
@@ -21,6 +21,9 @@
 #ifdef CONFIG_KSU_SUSFS
 #include <linux/susfs.h>
 #endif
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
 #ifdef CONFIG_KSU_SUSFS
 #include <linux/susfs_def.h>
 #include <linux/version.h>
@@ -226,6 +229,42 @@ int vfs_fstat(int fd, struct kstat *stat)
  * 0 will be returned on success, and a -ve error code if unsuccessful.
  */
 
+#ifdef CONFIG_ZEROMOUNT
+static inline int zeromount_stat_hook(int dfd, const char *kname, 
+                                      struct kstat *stat, unsigned int request_mask, 
+                                      int flags) {
+    if (zm_is_recursive() || IS_ERR_OR_NULL(kname)) return -ENOENT;
+    if (kname[0] != '/') {
+        char *abs_path = zeromount_build_absolute_path(dfd, kname);
+        if (abs_path) {
+            char *resolved = zeromount_resolve_path(abs_path);
+            if (resolved) {
+                struct path zm_path;
+                int zm_ret;
+                zm_enter();
+                zm_ret = kern_path(resolved, (flags & AT_SYMLINK_NOFOLLOW) ? 0 : LOOKUP_FOLLOW, &zm_path);
+                zm_exit();
+                kfree(resolved);
+                kfree(abs_path);
+                if (zm_ret == 0) {
+                    zm_ret = vfs_getattr(&zm_path, stat, request_mask,
+                                         (flags & AT_SYMLINK_NOFOLLOW) ? AT_SYMLINK_NOFOLLOW : 0);
+                    path_put(&zm_path);
+                    return zm_ret;
+                }
+            } else {
+                kfree(abs_path);
+            }
+        }
+    }
+    return -ENOENT;
+}
+#endif
 
 #ifdef CONFIG_KSU_SUSFS
 extern struct static_key_true ksu_su_compat_enabled;
@@ -262,6 +301,16 @@ static int vfs_statx(int dfd, const char __user *filename, int flags,
 	}
 
 orig_flow:
+#ifdef CONFIG_ZEROMOUNT
+	/* Try ZeroMount hook for relative paths using the resolved kernel string */
+	if (fname && !IS_ERR(fname)) {
+		int zm_ret = zeromount_stat_hook(dfd, fname->name, stat, request_mask, flags);
+		if (zm_ret != -ENOENT) {
+			putname(fname);
+			return zm_ret;
+		}
+	}
+#endif
 	error = filename_lookup(dfd, fname, lookup_flags, &path, NULL);
 	// no putname(fname) here as filename_lookup() has it done for us already;
 #else
diff --git a/fs/statfs.c b/fs/statfs.c
--- a/fs/statfs.c
+++ b/fs/statfs.c
@@ -14,6 +14,9 @@
 #include "mount.h"
 #endif // #if defined(CONFIG_KSU_SUSFS_SUS_MOUNT) || defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)
 
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
 #include "internal.h"
 
 static int flags_by_mnt(int mnt_flags)
@@ -148,7 +151,14 @@ int user_statfs(const char __user *pathname, struct kstatfs *st)
 retry:
 	error = user_path_at(AT_FDCWD, pathname, lookup_flags, &path);
 	if (!error) {
+#ifdef CONFIG_ZEROMOUNT
+		int spoofed;
+#endif
 		error = vfs_statfs(&path, st);
+#ifdef CONFIG_ZEROMOUNT
+		spoofed = zeromount_spoof_statfs(pathname, st);
+		(void)spoofed;
+#endif
 		path_put(&path);
 		if (retry_estale(error, lookup_flags)) {
 			lookup_flags |= LOOKUP_REVAL;
diff --git a/fs/xattr.c b/fs/xattr.c
--- a/fs/xattr.c
+++ b/fs/xattr.c
@@ -25,6 +25,9 @@
 #include <linux/posix_acl_xattr.h>
 
 #include <linux/uaccess.h>
+#ifdef CONFIG_ZEROMOUNT
+#include <linux/zeromount.h>
+#endif
 
 static const char *
 strcmp_prefix(const char *a, const char *a_prefix)
@@ -404,6 +407,12 @@ EXPORT_SYMBOL(__vfs_getxattr);
 ssize_t
 vfs_getxattr(struct dentry *dentry, const char *name, void *value, size_t size)
 {
+#ifdef CONFIG_ZEROMOUNT
+	ssize_t zm_ret;
+	zm_ret = zeromount_spoof_xattr(dentry, name, value, size);
+	if (zm_ret != -EOPNOTSUPP)
+		return zm_ret;
+#endif
 	return __vfs_getxattr(dentry, dentry->d_inode, name, value, size, 0);
 }
 EXPORT_SYMBOL_NS_GPL(vfs_getxattr, ANDROID_GKI_VFS_EXPORT_ONLY);
