name: build-kernel

on:
  schedule:
    - cron: "0 22 * * *" # Daily 3:30 AM IST (22:00 UTC)
  workflow_dispatch:
    inputs:
      publish_release:
        description: 'Publish the build as a GitHub release'
        type: boolean
        default: true
      release_tag:
        description: 'Release tag (auto-generated as kernel-YYYYMMDD when empty)'
        required: false
        default: ""
      kernel_repo:
        description: 'Kernel source repository (full URL)'
        required: false
        default: "https://github.com/Chor-A/Templar-Kernel-GKI-5.10"
      kernel_branch:
        description: 'Kernel source branch'
        required: false
        default: "Linux7-Staging"
      kernel_commit:
        description: 'Kernel source commit'
        required: false
        default: ""
      clang_version:
        description: Clang toolchain version
        required: false
        type: choice
        options:
          - clang 19
          - clang 14
        default: "clang 19"
      defconfig_fragment_exclude:
        description: 'Defconfig fragments to exclude'
        required: false
        default: ""
      append_build_env:
        description: 'Extra KEY=VALUE pairs appended to build.config'
        required: false
        default: ""
      append_config:
        description: 'Extra literal CONFIG_* lines injected into the defconfig'
        required: false
        default: "CONFIG_KSU=y,CONFIG_KSU_NEXT=y,CONFIG_OVERLAY_FS=y,CONFIG_KPROBES=y,CONFIG_HAVE_KPROBES=y,CONFIG_KPROBE_EVENTS=y,CONFIG_LRU_GEN=y,CONFIG_LRU_GEN_ENABLED=y,CONFIG_ZEROMOUNT=y"
      ccache:
        description: 'Accelerate with ccache.'
        type: boolean
        default: true

permissions:
  contents: write
  actions: write

concurrency:
  group: kernel-build-${{ github.event.inputs.release_tag || 'default' }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      KERNEL_REPO: ${{ inputs.kernel_repo }}
      KERNEL_BRANCH: ${{ inputs.kernel_branch }}
      KERNEL_COMMIT: ${{ inputs.kernel_commit }}
      CLANG_ASSET: ${{ fromJSON('{"clang 14":"clang-r450784e-linux-x86.tar.zst","clang 19":"clang-r536225-linux-x86.tar.zst"}')[inputs.clang_version] }}
      DEFCONFIG_FRAGMENT_EXCLUDE: ${{ inputs.defconfig_fragment_exclude }}
      APPEND_BUILD_ENV: ${{ inputs.append_build_env }}
      APPEND_CONFIG: ${{ inputs.append_config }}
      RELEASE_TAG: ${{ inputs.release_tag }}
      CCACHE_OPT: ${{ inputs.ccache && '1' || '' }}
      GITHUB_TOKEN: ${{ github.token }}
      KMI_SYMBOL_LIST_STRICT_MODE: "0"
      UPDATE_SYMBOL_LIST: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Free disk space
        run: |
          if [ -f "build/free-disk-space.sh" ]; then
            sudo bash build/free-disk-space.sh
          fi
          df -h

      - name: Configure Git Safe Directory
        run: |
          git config --global --add safe.directory "*"

      - name: Restore ccache cache
        if: ${{ inputs.ccache }}
        uses: actions/cache@v4
        with:
          path: work/.ccache
          key: cfx-ccache-${{ env.CLANG_ASSET }}-${{ inputs.kernel_repo }}-${{ inputs.kernel_branch }}-${{ github.run_id }}
          restore-keys: |
            cfx-ccache-${{ env.CLANG_ASSET }}-${{ inputs.kernel_repo }}-${{ inputs.kernel_branch }}-
            cfx-ccache-${{ env.CLANG_ASSET }}-${{ inputs.kernel_repo }}-
          fail-on-cache-miss: false

      - name: Add 24G swap
        run: |
          sudo swapoff -a || true
          sudo rm -f /swapfile
          sudo fallocate -l 24G /swapfile || sudo dd if=/dev/zero of=/swapfile bs=1M count=24576
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          free -h

      - name: Clone Kernel & Clean Hardcoded NoMount
        run: |
          mkdir -p work
          git clone -b ${{ env.KERNEL_BRANCH }} ${{ env.KERNEL_REPO }} work/common
          cd work/common

          if [ -n "${{ env.KERNEL_COMMIT }}" ]; then
            git checkout ${{ env.KERNEL_COMMIT }}
          fi

          echo "=== Reverting Hardcoded NoMount (Commit 3be84ff) ==="
          git config --global user.email "Antonio_Nelson@proton.me"
          git config --global user.name "Chor-A"
          
          git revert --no-edit 3be84ff || echo "Warning: Commit 3be84ff not found or already reverted. Proceeding."

      - name: Apply SUSFS, Custom Patches, ZeroMount & Direct Code Spoofing
        run: |
          cd work/common

          echo "=== 1. Integrating SUSFS into Kernel ==="
          git clone --depth=1 -b gki-android12-5.10 https://gitlab.com/simonpunk/susfs4ksu.git ../susfs4ksu
          cp -r ../susfs4ksu/kernel_patches/fs/* fs/
          cp -r ../susfs4ksu/kernel_patches/include/linux/* include/linux/
          patch -p1 < ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android12-5.10.patch || echo "Warning: SUSFS patch issue"

          echo "=== 2. Applying Custom Patches (mglru, services, etc.) ==="
          for dir in "$GITHUB_WORKSPACE/.github/workflows/patches" "$GITHUB_WORKSPACE/patches"; do
            if [ -d "$dir" ]; then
              for patch_file in "$dir"/*.patch; do
                if [ -f "$patch_file" ]; then
                  echo "Applying $patch_file..."
                  sed -i 's/\r$//' "$patch_file"
                  if git apply --verbose --ignore-whitespace --whitespace=nowarn "$patch_file"; then
                    echo "Applied via git apply"
                  elif patch -p1 -N --no-backup-if-mismatch < "$patch_file"; then
                    echo "Applied via patch"
                  else
                    echo "Warning: $patch_file partially skipped or failed"
                  fi
                fi
              done
            fi
          done
          echo "✅ All .patch files applied!"

          echo "=== 3. Injecting ZeroMount Subsystem via Python (Bulletproof) ==="
          cat << 'EOF_ZM_H' > include/linux/zeromount.h
          #ifndef _LINUX_ZEROMOUNT_H
          #define _LINUX_ZEROMOUNT_H

          #include <linux/types.h>
          #include <linux/list.h>
          #include <linux/hashtable.h>
          #include <linux/spinlock.h>
          #include <linux/limits.h>
          #include <linux/atomic.h>
          #include <linux/uidgid.h>
          #include <linux/stat.h>
          #include <linux/ioctl.h>
          #include <linux/rcupdate.h>
          #include <linux/printk.h>
          #include <linux/sched.h>
          #include <linux/bitops.h>

          #define ZM_BLOOM_BITS     4096
          #define ZM_BLOOM_SHIFT    12
          #define ZM_BLOOM_MASK     (ZM_BLOOM_BITS - 1)

          #ifdef CONFIG_ANDROID_VENDOR_OEM_DATA
          #define ZM_RECURSIVE_BIT 0
          static inline void zm_enter(void) { set_bit(ZM_RECURSIVE_BIT, (unsigned long *)&current->android_oem_data1); }
          static inline void zm_exit(void) { clear_bit(ZM_RECURSIVE_BIT, (unsigned long *)&current->android_oem_data1); }
          static inline bool zm_is_recursive(void) { return test_bit(ZM_RECURSIVE_BIT, (unsigned long *)&current->android_oem_data1); }
          #else
          #define ZM_RECURSIVE_MARKER ((void *)0x5A4D)
          static inline void zm_enter(void) { if (!current->journal_info) current->journal_info = ZM_RECURSIVE_MARKER; }
          static inline void zm_exit(void) { if (current->journal_info == ZM_RECURSIVE_MARKER) current->journal_info = NULL; }
          static inline bool zm_is_recursive(void) { return current->journal_info == ZM_RECURSIVE_MARKER; }
          #endif

          extern int zeromount_debug_level;
          #define ZM_LOG(level, fmt, ...) do { if (zeromount_debug_level >= (level) && printk_ratelimit()) pr_info("ZeroMount: " fmt, ##__VA_ARGS__); } while (0)
          #define ZM_TRACE(fmt, ...) ZM_LOG(3, fmt, ##__VA_ARGS__)
          #define ZM_DBG(fmt, ...)  ZM_LOG(2, fmt, ##__VA_ARGS__)
          #define ZM_INFO(fmt, ...) ZM_LOG(1, fmt, ##__VA_ARGS__)
          #define ZM_ERR(fmt, ...)  pr_err("ZeroMount: " fmt, ##__VA_ARGS__)

          #define ZEROMOUNT_MAGIC_CODE	0x5A
          #define ZEROMOUNT_VERSION	1
          #define ZEROMOUNT_HASH_BITS	10
          #define ZM_FLAG_ACTIVE		(1 << 0)
          #define ZM_FLAG_IS_DIR		(1 << 7)
          #define ZEROMOUNT_MAGIC_POS	0x7000000000000000ULL

          #define ZEROMOUNT_IOC_MAGIC	ZEROMOUNT_MAGIC_CODE
          #define ZEROMOUNT_IOC_ADD_RULE	_IOW(ZEROMOUNT_IOC_MAGIC, 1, struct zeromount_ioctl_data)
          #define ZEROMOUNT_IOC_DEL_RULE	_IOW(ZEROMOUNT_IOC_MAGIC, 2, struct zeromount_ioctl_data)
          #define ZEROMOUNT_IOC_CLEAR_ALL	_IO(ZEROMOUNT_IOC_MAGIC, 3)
          #define ZEROMOUNT_IOC_GET_VERSION _IOR(ZEROMOUNT_IOC_MAGIC, 4, int)
          #define ZEROMOUNT_IOC_ADD_UID	_IOW(ZEROMOUNT_IOC_MAGIC, 5, unsigned int)
          #define ZEROMOUNT_IOC_DEL_UID	_IOW(ZEROMOUNT_IOC_MAGIC, 6, unsigned int)
          #define ZEROMOUNT_IOC_GET_LIST	_IOR(ZEROMOUNT_IOC_MAGIC, 7, int)
          #define ZEROMOUNT_IOC_ENABLE	_IO(ZEROMOUNT_IOC_MAGIC, 8)
          #define ZEROMOUNT_IOC_DISABLE	_IO(ZEROMOUNT_IOC_MAGIC, 9)
          #define ZEROMOUNT_IOC_REFRESH	_IO(ZEROMOUNT_IOC_MAGIC, 10)
          #define ZEROMOUNT_IOC_GET_STATUS _IOR(ZEROMOUNT_IOC_MAGIC, 11, int)
          #define MAX_LIST_BUFFER_SIZE	(64 * 1024)

          struct zeromount_ioctl_data {
          	char __user *virtual_path;
          	char __user *real_path;
          	unsigned int flags;
          };

          struct zeromount_rule {
          	struct hlist_node node;
          	struct hlist_node ino_node;
          	struct list_head list;
          	size_t vp_len;
          	char *virtual_path;
          	char *real_path;
          	unsigned long real_ino;
          	dev_t real_dev;
          	unsigned long v_ino;
          	dev_t v_dev;
          	bool is_new;
          	u32 flags;
          	struct rcu_head rcu;
          };

          struct zeromount_dir_node {
          	struct hlist_node node;
          	char *dir_path;
          	struct list_head children_names;
          	struct rcu_head rcu;
          };

          struct zeromount_child_name {
          	struct list_head list;
          	char *name;
          	unsigned char d_type;
          	struct rcu_head rcu;
          };

          struct zeromount_uid_node {
          	uid_t uid;
          	struct hlist_node node;
          	struct rcu_head rcu;
          };

          extern DECLARE_HASHTABLE(zeromount_rules_ht, ZEROMOUNT_HASH_BITS);
          extern DECLARE_HASHTABLE(zeromount_dirs_ht, ZEROMOUNT_HASH_BITS);
          extern DECLARE_HASHTABLE(zeromount_uid_ht, ZEROMOUNT_HASH_BITS);
          extern DECLARE_HASHTABLE(zeromount_ino_ht, ZEROMOUNT_HASH_BITS);
          extern struct list_head zeromount_rules_list;
          extern spinlock_t zeromount_lock;

          #ifdef CONFIG_ZEROMOUNT
          extern atomic_t zeromount_enabled;
          bool zeromount_should_skip(void);
          char *zeromount_resolve_path(const char *pathname);
          char *zeromount_build_absolute_path(int dfd, const char *name);
          struct filename *zeromount_getname_hook(struct filename *name);
          void zeromount_inject_dents_common(struct file *file, void __user **dirent, int *count, loff_t *pos, int compat);
          void zeromount_inject_dents64(struct file *file, void __user **dirent, int *count, loff_t *pos);
          void zeromount_inject_dents(struct file *file, void __user **dirent, int *count, loff_t *pos);
          char *zeromount_get_virtual_path_for_inode(struct inode *inode);
          char *zeromount_get_static_vpath(struct inode *inode);
          void zeromount_spoof_mmap_metadata(struct inode *inode, dev_t *dev, unsigned long *ino);
          bool zeromount_is_traversal_allowed(struct inode *inode, int mask);
          bool zeromount_is_injected_file(struct inode *inode);
          bool zeromount_is_uid_blocked(uid_t uid);
          int zeromount_spoof_statfs(const char __user *pathname, struct kstatfs *buf);
          ssize_t zeromount_spoof_xattr(struct dentry *dentry, const char *name, void *value, size_t size);
          #else
          static inline bool zeromount_should_skip(void) { return true; }
          static inline char *zeromount_resolve_path(const char *p) { return NULL; }
          static inline char *zeromount_build_absolute_path(int dfd, const char *name) { return NULL; }
          static inline struct filename *zeromount_getname_hook(struct filename *name) { return name; }
          static inline void zeromount_inject_dents_common(struct file *f, void __user **d, int *c, loff_t *p, int compat) {}
          static inline void zeromount_inject_dents64(struct file *f, void __user **d, int *c, loff_t *p) {}
          static inline void zeromount_inject_dents(struct file *f, void __user **d, int *c, loff_t *p) {}
          static inline char *zeromount_get_virtual_path_for_inode(struct inode *inode) { return NULL; }
          static inline char *zeromount_get_static_vpath(struct inode *inode) { return NULL; }
          static inline void zeromount_spoof_mmap_metadata(struct inode *inode, dev_t *dev, unsigned long *ino) {}
          static inline bool zeromount_is_traversal_allowed(struct inode *inode, int mask) { return false; }
          static inline bool zeromount_is_injected_file(struct inode *inode) { return false; }
          static inline bool zeromount_is_uid_blocked(uid_t uid) { return false; }
          static inline int zeromount_spoof_statfs(const char __user *p, struct kstatfs *b) { return 0; }
          static inline ssize_t zeromount_spoof_xattr(struct dentry *d, const char *n, void *v, size_t s) { return -EOPNOTSUPP; }
          #endif
          #endif /* _LINUX_ZEROMOUNT_H */
          EOF_ZM_H

          cat << 'EOF_ZM_C' > fs/zeromount.c
          #include <linux/module.h>
          #include <linux/kernel.h>
          #include <linux/init.h>
          #include <linux/fs.h>
          #include <linux/dcache.h>
          #include <linux/path.h>
          #include <linux/namei.h>
          #include <linux/sched.h>
          #include <linux/slab.h>
          #include <linux/string.h>
          #include <linux/uaccess.h>
          #include <linux/dirent.h>
          #include <linux/miscdevice.h>
          #include <linux/cred.h>
          #include <linux/vmalloc.h>
          #include <linux/mm.h>
          #include <linux/zeromount.h>
          #include <linux/kobject.h>
          #include <linux/sysfs.h>
          #include <linux/statfs.h>
          #include <linux/file.h>
          #include <linux/fs_struct.h>
          #ifdef CONFIG_KSU_SUSFS
          #include <linux/susfs.h>
          #endif
          #include <linux/reboot.h>
          #include <linux/bitmap.h>

          int zeromount_debug_level = 0;

          DEFINE_HASHTABLE(zeromount_rules_ht, ZEROMOUNT_HASH_BITS);
          DEFINE_HASHTABLE(zeromount_dirs_ht, ZEROMOUNT_HASH_BITS);
          DEFINE_HASHTABLE(zeromount_uid_ht, ZEROMOUNT_HASH_BITS);
          DEFINE_HASHTABLE(zeromount_ino_ht, ZEROMOUNT_HASH_BITS);
          LIST_HEAD(zeromount_rules_list);
          DEFINE_SPINLOCK(zeromount_lock);

          atomic_t zeromount_enabled = ATOMIC_INIT(0);
          static atomic_t zeromount_dirs_count = ATOMIC_INIT(0);
          static atomic_t zeromount_rule_count = ATOMIC_INIT(0);
          static DECLARE_BITMAP(zm_bloom, ZM_BLOOM_BITS);
          #define ZEROMOUNT_DISABLED() (atomic_read(&zeromount_enabled) == 0)

          static inline void zm_bloom_add(u32 hash)
          {
          	set_bit(hash & ZM_BLOOM_MASK, zm_bloom);
          	set_bit((hash >> 10) & ZM_BLOOM_MASK, zm_bloom);
          	set_bit((hash >> 20) & ZM_BLOOM_MASK, zm_bloom);
          }

          static inline bool zm_bloom_test(u32 hash)
          {
          	return test_bit(hash & ZM_BLOOM_MASK, zm_bloom) &&
          	       test_bit((hash >> 10) & ZM_BLOOM_MASK, zm_bloom) &&
          	       test_bit((hash >> 20) & ZM_BLOOM_MASK, zm_bloom);
          }

          static void zm_bloom_rebuild(void)
          {
          	struct zeromount_rule *rule;
          	int count = 0;

          	bitmap_zero(zm_bloom, ZM_BLOOM_BITS);
          	list_for_each_entry(rule, &zeromount_rules_list, list) {
          		u32 h = full_name_hash(NULL, rule->virtual_path, rule->vp_len);
          		zm_bloom_add(h);
          		count++;
          	}
          	ZM_DBG("bloom: rebuilt (%d rules)\n", count);
          }

          struct linux_dirent {
          	unsigned long	d_ino;
          	unsigned long	d_off;
          	unsigned short	d_reclen;
          	char		d_name[];
          };

          static unsigned long zm_ino_adb = 0;
          static unsigned long zm_ino_modules = 0;

          static inline bool zeromount_is_critical_process(void)
          {
          	const char *comm = current->comm;

          	switch (comm[0]) {
          	case 'i': if (comm[1] == 'n' && comm[2] == 'i') return true; break;
          	case 'u': if (comm[1] == 'e' && comm[2] == 'v') return true; break;
          	case 'v': if (comm[1] == 'o' && comm[2] == 'l') return true; break;
          	}
          	if (current->flags & PF_KTHREAD)
          		return true;
          	return false;
          }

          bool zeromount_should_skip(void)
          {
          	if (ZEROMOUNT_DISABLED())
          		return true;
          	if (zm_is_recursive())
          		return true;
          	if (unlikely(in_interrupt() || in_nmi() || oops_in_progress))
          		return true;
          	if (!current || !current->mm)
          		return true;
          	if (current->flags & PF_EXITING)
          		return true;
          	if (zeromount_is_critical_process())
          		return true;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted())
          		return true;
          #endif
          	return false;
          }
          EXPORT_SYMBOL(zeromount_should_skip);

          bool zeromount_is_uid_blocked(uid_t uid)
          {
          	struct zeromount_uid_node *entry;

          	if (ZEROMOUNT_DISABLED())
          		return false;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted()) return true;
          #endif

          	rcu_read_lock();
          	hash_for_each_possible_rcu(zeromount_uid_ht, entry, node, uid) {
          		if (entry->uid == uid) {
          			rcu_read_unlock();
          			return true;
          		}
          	}
          	rcu_read_unlock();
          	return false;
          }
          EXPORT_SYMBOL(zeromount_is_uid_blocked);

          bool zeromount_match_path(const char *input_path, const char *rule_path)
          {
          	if (!input_path || !rule_path)
          		return false;
          	if (strcmp(input_path, rule_path) == 0)
          		return true;
          	if (strncmp(input_path, "/system", 7) == 0) {
          		if (strcmp(input_path + 7, rule_path) == 0)
          			return true;
          	}
          	return false;
          }

          static inline void zeromount_normalize_inline(const char *path, const char **out_p, size_t *out_len)
          {
          	const char *p = path;
          	size_t len;

          	if (strncmp(path, "/system/", 8) == 0)
          		p = path + 7;

          	len = strlen(p);
          	while (len > 1 && p[len - 1] == '/')
          		len--;

          	*out_p = p;
          	*out_len = len;
          }

          static char *zeromount_normalize_path(const char *path)
          {
          	const char *p;
          	size_t len;
          	char *normalized;

          	if (!path)
          		return NULL;

          	zeromount_normalize_inline(path, &p, &len);

          	normalized = kmalloc(len + 1, GFP_KERNEL);
          	if (!normalized)
          		return NULL;

          	memcpy(normalized, p, len);
          	normalized[len] = '\0';
          	return normalized;
          }

          static void zeromount_free_rule_rcu(struct rcu_head *head)
          {
          	struct zeromount_rule *rule = container_of(head, struct zeromount_rule, rcu);

          	kfree(rule->virtual_path);
          	kfree(rule->real_path);
          	kfree(rule);
          }

          static void zeromount_free_child_rcu(struct rcu_head *head)
          {
          	struct zeromount_child_name *child =
          		container_of(head, struct zeromount_child_name, rcu);

          	kfree(child->name);
          	kfree(child);
          }

          static void zeromount_free_dir_node_rcu(struct rcu_head *head)
          {
          	struct zeromount_dir_node *node =
          		container_of(head, struct zeromount_dir_node, rcu);

          	kfree(node->dir_path);
          	kfree(node);
          }

          static void zeromount_flush_parent(const char *full_path)
          {
          	char *path_copy, *last_slash, *parent_str, *child_name;
          	struct path parent;

          	path_copy = kstrdup(full_path, GFP_KERNEL);
          	if (!path_copy)
          		return;

          	last_slash = strrchr(path_copy, '/');
          	if (!last_slash || last_slash == path_copy) {
          		kfree(path_copy);
          		return;
          	}

          	*last_slash = '\0';
          	parent_str = path_copy;
          	child_name = last_slash + 1;

          	if (*child_name == '\0') {
          		kfree(path_copy);
          		return;
          	}

          	if (kern_path(parent_str, LOOKUP_FOLLOW, &parent) == 0) {
          		struct dentry *child;

          		inode_lock(parent.dentry->d_inode);
          		child = lookup_one_len(child_name, parent.dentry,
          				       strlen(child_name));
          		if (!IS_ERR(child)) {
          			d_invalidate(child);
          			d_drop(child);
          			dput(child);
          		}
          		inode_unlock(parent.dentry->d_inode);
          		path_put(&parent);
          	}

          	kfree(path_copy);
          }

          static void zeromount_flush_dcache(const char *path_name)
          {
          	struct path path;
          	int err;

          	zm_enter();
          	err = kern_path(path_name, LOOKUP_FOLLOW, &path);
          	if (!err) {
          		d_invalidate(path.dentry);
          		d_drop(path.dentry);
          		path_put(&path);
          	} else if (err == -ENOENT) {
          		zeromount_flush_parent(path_name);
          	}
          	zm_exit();
          }

          static void zeromount_force_refresh_all(void)
          {
          	struct zeromount_rule *rule;
          	char **paths = NULL;
          	int count = 0, i = 0;

          	spin_lock(&zeromount_lock);
          	list_for_each_entry(rule, &zeromount_rules_list, list)
          		count++;
          	spin_unlock(&zeromount_lock);

          	if (count == 0)
          		return;

          	paths = kvmalloc_array(count, sizeof(char *), GFP_KERNEL);
          	if (!paths)
          		return;

          	spin_lock(&zeromount_lock);
          	list_for_each_entry(rule, &zeromount_rules_list, list) {
          		if (i >= count)
          			break;
          		paths[i] = kstrdup(rule->virtual_path, GFP_ATOMIC);
          		if (!paths[i])
          			break;
          		i++;
          	}
          	spin_unlock(&zeromount_lock);

          	for (count = i, i = 0; i < count; i++) {
          		zeromount_flush_dcache(paths[i]);
          		kfree(paths[i]);
          	}
          	kvfree(paths);
          }

          static unsigned long zeromount_generate_ino(const char *dir, const char *name)
          {
          	u32 h1 = full_name_hash(NULL, dir, strlen(dir));
          	u32 h2 = full_name_hash(NULL, name, strlen(name));

          	return (unsigned long)(h1 ^ h2);
          }

          char *zeromount_get_virtual_path_for_inode(struct inode *inode)
          {
          	struct zeromount_rule *rule;
          	unsigned long key;
          	char *found_path = NULL;

          	if (!inode || !inode->i_sb || zeromount_should_skip())
          		return NULL;
          	if (zeromount_is_uid_blocked(current_uid().val))
          		return NULL;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted())
          		return NULL;
          #endif

          	key = inode->i_ino ^ inode->i_sb->s_dev;

          	if (atomic_read(&zeromount_rule_count) == 0)
          		return NULL;

          	rcu_read_lock();
          	hash_for_each_possible_rcu(zeromount_ino_ht, rule, ino_node, key) {
          		if (rule->real_ino == inode->i_ino &&
          		    rule->real_dev == inode->i_sb->s_dev) {
          			if (READ_ONCE(rule->is_new))
          				found_path = kstrdup(rule->virtual_path,
          						     GFP_ATOMIC);
          			break;
          		}
          	}
          	rcu_read_unlock();
          	return found_path;
          }
          EXPORT_SYMBOL(zeromount_get_virtual_path_for_inode);

          char *zeromount_get_static_vpath(struct inode *inode)
          {
          	struct zeromount_rule *rule;
          	unsigned long key;
          	char *copy = NULL;

          	if (unlikely(!inode || !inode->i_sb))
          		return NULL;

          	key = inode->i_ino ^ inode->i_sb->s_dev;

          	rcu_read_lock();
          	hash_for_each_possible_rcu(zeromount_ino_ht, rule, ino_node, key) {
          		if (rule->real_ino == inode->i_ino &&
          		    rule->real_dev == inode->i_sb->s_dev &&
          		    (rule->flags & ZM_FLAG_ACTIVE)) {
          			copy = kstrdup(rule->virtual_path, GFP_ATOMIC);
          			break;
          		}
          	}
          	rcu_read_unlock();
          	return copy;
          }
          EXPORT_SYMBOL(zeromount_get_static_vpath);

          void zeromount_spoof_mmap_metadata(struct inode *inode, dev_t *dev, unsigned long *ino)
          {
          	struct zeromount_rule *rule;
          	unsigned long key;

          	if (unlikely(!inode || !dev || !ino))
          		return;
          	if (zeromount_should_skip())
          		return;

          	key = inode->i_ino ^ inode->i_sb->s_dev;

          	rcu_read_lock();
          	hash_for_each_possible_rcu(zeromount_ino_ht, rule, ino_node, key) {
          		if (rule->real_ino == inode->i_ino &&
          		    rule->real_dev == inode->i_sb->s_dev &&
          		    (rule->flags & ZM_FLAG_ACTIVE)) {
          			*dev = READ_ONCE(rule->v_dev);
          			*ino = READ_ONCE(rule->v_ino);
          			break;
          		}
          	}
          	rcu_read_unlock();
          }
          EXPORT_SYMBOL(zeromount_spoof_mmap_metadata);

          static unsigned long zeromount_get_inode_by_path(const char *path_str)
          {
          	struct path path;
          	struct inode *inode;
          	unsigned long ino = 0;

          	if (kern_path(path_str, LOOKUP_FOLLOW, &path) == 0) {
          		inode = d_backing_inode(path.dentry);
          		if (inode)
          			ino = inode->i_ino;
          		path_put(&path);
          	}
          	return ino;
          }

          static void zeromount_refresh_critical_inodes(void)
          {
          	if (READ_ONCE(zm_ino_adb) == 0) {
          		unsigned long ino = zeromount_get_inode_by_path("/data/adb");
          		if (ino != 0)
          			WRITE_ONCE(zm_ino_adb, ino);
          	}
          	if (READ_ONCE(zm_ino_modules) == 0) {
          		unsigned long ino = zeromount_get_inode_by_path("/data/adb/modules");
          		if (ino != 0)
          			WRITE_ONCE(zm_ino_modules, ino);
          	}
          }

          bool zeromount_is_traversal_allowed(struct inode *inode, int mask)
          {
          	if (!inode || zeromount_should_skip() ||
          	    zeromount_is_uid_blocked(current_uid().val))
          		return false;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted()) return false;
          #endif
          	if (!(mask & MAY_EXEC))
          		return false;

          	if ((READ_ONCE(zm_ino_adb) != 0 &&
          	     inode->i_ino == READ_ONCE(zm_ino_adb)) ||
          	    (READ_ONCE(zm_ino_modules) != 0 &&
          	     inode->i_ino == READ_ONCE(zm_ino_modules)))
          		return true;

          	return false;
          }
          EXPORT_SYMBOL(zeromount_is_traversal_allowed);

          bool zeromount_is_injected_file(struct inode *inode)
          {
          	struct zeromount_rule *rule;
          	unsigned long key;

          	if (!inode || !inode->i_sb || zeromount_should_skip())
          		return false;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted())
          		return false;
          #endif

          	key = inode->i_ino ^ inode->i_sb->s_dev;

          	if (atomic_read(&zeromount_rule_count) == 0)
          		return false;

          	rcu_read_lock();
          	hash_for_each_possible_rcu(zeromount_ino_ht, rule, ino_node, key) {
          		if (rule->real_ino == inode->i_ino &&
          		    rule->real_dev == inode->i_sb->s_dev) {
          			rcu_read_unlock();
          			return true;
          		}
          	}
          	rcu_read_unlock();
          	return false;
          }
          EXPORT_SYMBOL(zeromount_is_injected_file);

          char *zeromount_resolve_path(const char *pathname)
          {
          	struct zeromount_rule *rule;
          	char *target = NULL;
          	const char *normalized;
          	size_t norm_len;
          	u32 hash;

          	if (zeromount_is_critical_process())
          		return NULL;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted())
          		return NULL;
          #endif
          	if (ZEROMOUNT_DISABLED() ||
          	    zeromount_is_uid_blocked(current_uid().val) || !pathname)
          		return NULL;

          	zeromount_normalize_inline(pathname, &normalized, &norm_len);
          	hash = full_name_hash(NULL, normalized, norm_len);

          	if (atomic_read(&zeromount_rule_count) == 0)
          		return NULL;

          	if (!zm_bloom_test(hash)) {
          		ZM_TRACE("bloom: reject hash=0x%08x path=%.*s\n", hash, (int)norm_len, normalized);
          		return NULL;
          	}
          	ZM_DBG("bloom: pass hash=0x%08x path=%.*s\n", hash, (int)norm_len, normalized);

          	rcu_read_lock();
          	hash_for_each_possible_rcu(zeromount_rules_ht, rule, node, hash) {
          		if (rule->vp_len == norm_len &&
          		    memcmp(normalized, rule->virtual_path, norm_len) == 0) {
          			if (rule->flags & ZM_FLAG_ACTIVE) {
          				target = kstrdup(rule->real_path, GFP_ATOMIC);
          				break;
          			}
          		}
          	}
          	rcu_read_unlock();
          	return target;
          }
          EXPORT_SYMBOL(zeromount_resolve_path);

          static char *zeromount_resolve_dirfd_path(int dfd, char *buf, int buflen)
          {
          	struct fd f;
          	char *path;

          	if (dfd == AT_FDCWD) {
          		struct path pwd;

          		if (unlikely(!current->fs))
          			return ERR_PTR(-ENOENT);
          		get_fs_pwd(current->fs, &pwd);
          		path = d_path(&pwd, buf, buflen);
          		path_put(&pwd);
          		return path;
          	}

          	f = fdget(dfd);
          	if (!f.file)
          		return ERR_PTR(-EBADF);

          	path = d_path(&f.file->f_path, buf, buflen);
          	fdput(f);
          	return path;
          }

          char *zeromount_build_absolute_path(int dfd, const char *name)
          {
          	char *page_buf, *dir_path, *abs_path;
          	size_t dir_len, name_len;

          	if (!name || name[0] == '/' || *name == '\0')
          		return NULL;
          	if (zeromount_should_skip() ||
          	    zeromount_is_uid_blocked(current_uid().val))
          		return NULL;

          	page_buf = __getname();
          	if (!page_buf)
          		return NULL;

          	dir_path = zeromount_resolve_dirfd_path(dfd, page_buf, PATH_MAX);
          	if (IS_ERR(dir_path)) {
          		__putname(page_buf);
          		return NULL;
          	}

          	dir_len = strlen(dir_path);
          	name_len = strlen(name);

          	if (dir_len > PATH_MAX || name_len > NAME_MAX ||
          	    dir_len + name_len + 2 > PATH_MAX) {
          		__putname(page_buf);
          		return NULL;
          	}

          	abs_path = kmalloc(dir_len + 1 + name_len + 1, GFP_KERNEL);
          	if (abs_path) {
          		memcpy(abs_path, dir_path, dir_len);
          		abs_path[dir_len] = '/';
          		memcpy(abs_path + dir_len + 1, name, name_len + 1);
          	}

          	__putname(page_buf);
          	return abs_path;
          }
          EXPORT_SYMBOL(zeromount_build_absolute_path);

          struct filename *zeromount_getname_hook(struct filename *name)
          {
          	char *target_path;
          	struct filename *new_name;

          	if (zeromount_should_skip() ||
          	    zeromount_is_uid_blocked(current_uid().val) ||
          	    !name || name->name[0] != '/')
          		return name;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted())
          		return name;
          #endif

          	zm_enter();

          	target_path = zeromount_resolve_path(name->name);
          	if (!target_path) {
          		zm_exit();
          		return name;
          	}

          	new_name = getname_kernel(target_path);
          	kfree(target_path);

          	if (IS_ERR(new_name)) {
          		zm_exit();
          		return name;
          	}

          	putname(name);
          	zm_exit();
          	return new_name;
          }

          void zeromount_inject_dents_common(struct file *file, void __user **dirent,
          				   int *count, loff_t *pos, int compat)
          {
          	char *page_buf, *dir_path;
          	unsigned long v_index;

          	if (zeromount_should_skip() ||
          	    zeromount_is_uid_blocked(current_uid().val))
          		return;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted()) return;
          #endif

          	if (atomic_read(&zeromount_dirs_count) == 0 &&
          	    *pos < ZEROMOUNT_MAGIC_POS)
          		return;

          	page_buf = __getname();
          	if (!page_buf)
          		return;

          	dir_path = d_path(&file->f_path, page_buf, PAGE_SIZE);
          	if (IS_ERR(dir_path)) {
          		__putname(page_buf);
          		return;
          	}

          	{
          		const char *norm_p;
          		size_t norm_len;
          		u32 dir_hash;
          		bool is_redirected = false;

          		zeromount_normalize_inline(dir_path, &norm_p, &norm_len);
          		dir_hash = full_name_hash(NULL, norm_p, norm_len);

          		rcu_read_lock();
          		{
          			struct zeromount_rule *rule;

          			hash_for_each_possible_rcu(zeromount_rules_ht, rule,
          						   node, dir_hash) {
          				if (rule->vp_len == norm_len &&
          				    memcmp(norm_p, rule->virtual_path,
          					   norm_len) == 0 &&
          				    (rule->flags & ZM_FLAG_ACTIVE)) {
          					is_redirected = true;
          					break;
          				}
          			}
          		}
          		rcu_read_unlock();

          		if (is_redirected) {
          			__putname(page_buf);
          			return;
          		}
          	}

          	if (*pos >= ZEROMOUNT_MAGIC_POS) {
          		v_index = *pos - ZEROMOUNT_MAGIC_POS;
          	} else {
          		v_index = 0;
          	}

          	{
          		struct zeromount_dir_node *zm_dir;
          		struct zeromount_child_name *zm_child;
          		const char *normalized;
          		size_t normalized_len;
          		u32 zm_hash;
          		unsigned long cur_idx = 0;

          		zeromount_normalize_inline(dir_path, &normalized,
          					  &normalized_len);
          		zm_hash = full_name_hash(NULL, normalized, normalized_len);

          		rcu_read_lock();
          		hash_for_each_possible_rcu(zeromount_dirs_ht, zm_dir,
          					   node, zm_hash) {
          			if (strlen(zm_dir->dir_path) != normalized_len ||
          			    memcmp(normalized, zm_dir->dir_path,
          				   normalized_len) != 0)
          				continue;

          			if (*pos < ZEROMOUNT_MAGIC_POS)
          				*pos = ZEROMOUNT_MAGIC_POS;

          			list_for_each_entry_rcu(zm_child,
          						&zm_dir->children_names,
          						list) {
          				char name_local[256];
          				unsigned char type_local;
          				int name_len, reclen;
          				unsigned long fake_ino;

          				if (cur_idx++ < v_index)
          					continue;

          				strscpy(name_local, zm_child->name,
          					sizeof(name_local));
          				type_local = zm_child->d_type;
          				name_len = strlen(name_local);

          				if (compat) {
          					struct linux_dirent __user *de;

          					reclen = ALIGN(offsetof(struct linux_dirent, d_name) + name_len + 2, 4);
          					if (*count < reclen)
          						break;
          					de = (struct linux_dirent __user *)*dirent;
          					fake_ino = zeromount_generate_ino(dir_path, name_local);
          					if (put_user(fake_ino, &de->d_ino) ||
          					    put_user(ZEROMOUNT_MAGIC_POS + v_index + 1, &de->d_off) ||
          					    put_user(reclen, &de->d_reclen) ||
          					    copy_to_user(de->d_name, name_local, name_len) ||
          					    put_user(0, de->d_name + name_len) ||
          					    put_user(type_local, ((char __user *)de) + reclen - 1))
          						break;
          				} else {
          					struct linux_dirent64 __user *de64;

          					reclen = ALIGN(offsetof(struct linux_dirent64, d_name) + name_len + 1, sizeof(u64));
          					if (*count < reclen)
          						break;
          					de64 = (struct linux_dirent64 __user *)*dirent;
          					fake_ino = zeromount_generate_ino(dir_path, name_local);
          					if (put_user(fake_ino, &de64->d_ino) ||
          					    put_user(ZEROMOUNT_MAGIC_POS + v_index + 1, &de64->d_off) ||
          					    put_user(reclen, &de64->d_reclen) ||
          					    put_user(type_local, &de64->d_type) ||
          					    copy_to_user(de64->d_name, name_local, name_len) ||
          					    put_user(0, de64->d_name + name_len))
          						break;
          				}

          				*dirent = (void __user *)((char __user *)*dirent + reclen);
          				*count -= reclen;
          				v_index++;
          				*pos = ZEROMOUNT_MAGIC_POS + v_index;
          			}
          			break;
          		}
          		rcu_read_unlock();
          	}

          	__putname(page_buf);
          }

          void zeromount_inject_dents64(struct file *file, void __user **dirent,
          			      int *count, loff_t *pos)
          {
          	zeromount_inject_dents_common(file, dirent, count, pos, 0);
          }

          void zeromount_inject_dents(struct file *file, void __user **dirent,
          			    int *count, loff_t *pos)
          {
          	zeromount_inject_dents_common(file, dirent, count, pos, 1);
          }

          #define EROFS_SUPER_MAGIC 0xE0F5E1E2
          #define EXT4_SUPER_MAGIC  0xEF53
          #define F2FS_SUPER_MAGIC  0xF2F52010

          int zeromount_spoof_statfs(const char __user *pathname, struct kstatfs *buf)
          {
          	char *kpath;
          	char *resolved;
          	int ret = 0;

          	if (zeromount_should_skip() ||
          	    zeromount_is_uid_blocked(current_uid().val))
          		return 0;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted())
          		return 0;
          #endif

          	kpath = strndup_user(pathname, PATH_MAX);
          	if (IS_ERR(kpath))
          		return 0;

          	resolved = zeromount_resolve_path(kpath);
          	if (!resolved) {
          		kfree(kpath);
          		return 0;
          	}

          	if (strncmp(kpath, "/system", 7) == 0 ||
          	    strncmp(kpath, "/vendor", 7) == 0 ||
          	    strncmp(kpath, "/product", 8) == 0 ||
          	    strncmp(kpath, "/odm", 4) == 0) {
          		buf->f_type = EROFS_SUPER_MAGIC;
          		ret = 1;
          	}

          	kfree(kpath);
          	kfree(resolved);
          	return ret;
          }
          EXPORT_SYMBOL(zeromount_spoof_statfs);

          static const char *zeromount_get_selinux_context(const char *vpath)
          {
          	if (!vpath)
          		return NULL;

          	if (strncmp(vpath, "/lib64", 6) == 0 ||
          	    strncmp(vpath, "/lib", 4) == 0)
          		return "u:object_r:system_lib_file:s0";

          	if (strncmp(vpath, "/bin", 4) == 0)
          		return "u:object_r:system_file:s0";

          	if (strncmp(vpath, "/fonts", 6) == 0)
          		return "u:object_r:system_file:s0";

          	if (strncmp(vpath, "/framework", 10) == 0)
          		return "u:object_r:system_file:s0";

          	if (strncmp(vpath, "/etc", 4) == 0)
          		return "u:object_r:system_file:s0";

          	if (strncmp(vpath, "/vendor", 7) == 0)
          		return "u:object_r:vendor_file:s0";

          	if (strncmp(vpath, "/product", 8) == 0)
          		return "u:object_r:system_file:s0";

          	if (vpath[0] == '/')
          		return "u:object_r:system_file:s0";

          	return NULL;
          }

          ssize_t zeromount_spoof_xattr(struct dentry *dentry, const char *name,
          			      void *value, size_t size)
          {
          	struct inode *inode;
          	char *vpath;
          	const char *context;
          	size_t ctx_len;

          	if (zeromount_should_skip() ||
          	    zeromount_is_uid_blocked(current_uid().val))
          		return -EOPNOTSUPP;
          #ifdef CONFIG_KSU_SUSFS
          	if (susfs_is_current_proc_umounted())
          		return -EOPNOTSUPP;
          #endif

          	if (!dentry || !name)
          		return -EOPNOTSUPP;

          	if (strcmp(name, "security.selinux") != 0)
          		return -EOPNOTSUPP;

          	inode = d_backing_inode(dentry);
          	if (!inode)
          		return -EOPNOTSUPP;

          	vpath = zeromount_get_virtual_path_for_inode(inode);
          	if (!vpath)
          		return -EOPNOTSUPP;

          	context = zeromount_get_selinux_context(vpath);
          	kfree(vpath);

          	if (!context)
          		return -EOPNOTSUPP;

          	ctx_len = strlen(context) + 1;

          	if (size == 0)
          		return ctx_len;
          	if (size < ctx_len)
          		return -ERANGE;

          	memcpy(value, context, ctx_len);
          	return ctx_len;
          }
          EXPORT_SYMBOL(zeromount_spoof_xattr);

          static void zeromount_auto_inject_parent(const char *v_path,
          					 unsigned char type, int depth)
          {
          	char *parent_path, *name, *path_copy, *last_slash;
          	struct zeromount_dir_node *dir_node = NULL, *curr;
          	struct zeromount_child_name *child;
          	u32 hash;
          	bool child_exists = false;

          	if (depth > 20)
          		return;

          	{
          		struct path check;
          		if (kern_path(v_path, LOOKUP_FOLLOW, &check) == 0) {
          			path_put(&check);
          			return;
          		}
          	}

          	path_copy = kstrdup(v_path, GFP_KERNEL);
          	if (!path_copy)
          		return;

          	last_slash = strrchr(path_copy, '/');
          	if (!last_slash || last_slash == path_copy) {
          		kfree(path_copy);
          		return;
          	}

          	*last_slash = '\0';
          	parent_path = path_copy;
          	name = last_slash + 1;

          	{
          		u32 parent_hash = full_name_hash(NULL, parent_path,
          						 strlen(parent_path));
          		struct zeromount_rule *parent_rule;
          		bool parent_redirected = false;

          		rcu_read_lock();
          		hash_for_each_possible_rcu(zeromount_rules_ht, parent_rule,
          					   node, parent_hash) {
          			if (strcmp(parent_rule->virtual_path, parent_path) == 0 &&
          			    READ_ONCE(parent_rule->is_new)) {
          				parent_redirected = true;
          				break;
          			}
          		}
          		rcu_read_unlock();

          		if (parent_redirected) {
          			kfree(path_copy);
          			return;
          		}
          	}

          	zeromount_auto_inject_parent(parent_path, DT_DIR, depth + 1);

          	hash = full_name_hash(NULL, parent_path, strlen(parent_path));

          	spin_lock(&zeromount_lock);

          	hash_for_each_possible(zeromount_dirs_ht, curr, node, hash) {
          		if (strcmp(curr->dir_path, parent_path) == 0) {
          			dir_node = curr;
          			break;
          		}
          	}

          	if (!dir_node) {
          		dir_node = kzalloc(sizeof(*dir_node), GFP_ATOMIC);
          		if (!dir_node)
          			goto unlock_out;

          		dir_node->dir_path = kstrdup(parent_path, GFP_ATOMIC);
          		INIT_LIST_HEAD(&dir_node->children_names);
          		hash_add_rcu(zeromount_dirs_ht, &dir_node->node, hash);
          		atomic_inc(&zeromount_dirs_count);
          	}

          	list_for_each_entry(child, &dir_node->children_names, list) {
          		if (strcmp(child->name, name) == 0) {
          			child_exists = true;
          			break;
          		}
          	}

          	if (!child_exists) {
          		child = kzalloc(sizeof(*child), GFP_ATOMIC);
          		if (child) {
          			child->name = kstrdup(name, GFP_ATOMIC);
          			child->d_type = (type == DT_DIR) ? 4 : 8;
          			list_add_tail_rcu(&child->list,
          					  &dir_node->children_names);
          		}
          	}

          unlock_out:
          	spin_unlock(&zeromount_lock);
          	kfree(path_copy);
          }

          static int zeromount_ioctl_add_rule(unsigned long arg)
          {
          	struct zeromount_ioctl_data data;
          	struct zeromount_rule *rule;
          	char *v_path_raw, *v_path, *r_path;
          	struct path path;
          	unsigned char type;
          	u32 hash;

          	if (copy_from_user(&data, (void __user *)arg, sizeof(data)))
          		return -EFAULT;

          	v_path_raw = strndup_user(data.virtual_path, PATH_MAX);
          	if (IS_ERR(v_path_raw))
          		return PTR_ERR(v_path_raw);

          	v_path = zeromount_normalize_path(v_path_raw);
          	kfree(v_path_raw);
          	if (!v_path)
          		return -ENOMEM;

          	r_path = strndup_user(data.real_path, PATH_MAX);
          	if (IS_ERR(r_path)) {
          		kfree(v_path);
          		return PTR_ERR(r_path);
          	}

          	hash = full_name_hash(NULL, v_path, strlen(v_path));
          	rule = kzalloc(sizeof(*rule), GFP_KERNEL);
          	if (!rule) {
          		kfree(v_path);
          		kfree(r_path);
          		return -ENOMEM;
          	}

          	rule->virtual_path = v_path;
          	rule->vp_len = strlen(v_path);
          	rule->real_path = r_path;
          	rule->flags = data.flags | ZM_FLAG_ACTIVE;
          	rule->is_new = false;

          	if (zm_ino_adb == 0)
          		zeromount_refresh_critical_inodes();

          	if (kern_path(r_path, LOOKUP_FOLLOW, &path) == 0) {
          		struct inode *inode = d_backing_inode(path.dentry);

          		if (inode) {
          			rule->real_ino = inode->i_ino;
          			rule->real_dev = inode->i_sb->s_dev;
          		}
          		path_put(&path);
          	} else {
          		rule->real_ino = 0;
          		rule->real_dev = 0;
          	}

          	zm_enter();
          	if (kern_path(v_path, LOOKUP_FOLLOW, &path) == 0) {
          		struct inode *vinode = d_backing_inode(path.dentry);

          		if (vinode) {
          			rule->v_ino = vinode->i_ino;
          			rule->v_dev = vinode->i_sb->s_dev;
          		}
          		path_put(&path);
          	}
          	zm_exit();

          	if (rule->v_ino == 0) {
          		rule->v_ino = zeromount_generate_ino(v_path, r_path);
          		{
          			char *parent_copy = kstrdup(v_path, GFP_KERNEL);

          			if (parent_copy) {
          				char *slash = strrchr(parent_copy, '/');

          				if (slash && slash != parent_copy) {
          					*slash = '\0';
          					zm_enter();
          					if (kern_path(parent_copy, LOOKUP_FOLLOW,
          						      &path) == 0) {
          						struct inode *pinode =
          							d_backing_inode(path.dentry);
          						if (pinode)
          							rule->v_dev = pinode->i_sb->s_dev;
          						path_put(&path);
          					}
          					zm_exit();
          				}
          				kfree(parent_copy);
          			}
          		}
          	}

          	spin_lock(&zeromount_lock);
          	hash_add_rcu(zeromount_rules_ht, &rule->node, hash);
          	zm_bloom_add(hash);
          	ZM_DBG("bloom: add hash=0x%08x path=%s\n", hash, rule->virtual_path);
          	if (rule->real_ino != 0) {
          		unsigned long ino_key = rule->real_ino ^ rule->real_dev;

          		hash_add_rcu(zeromount_ino_ht, &rule->ino_node, ino_key);
          	}
          	list_add_tail(&rule->list, &zeromount_rules_list);
          	atomic_inc(&zeromount_rule_count);
          	spin_unlock(&zeromount_lock);

          	type = DT_REG;
          	if (data.flags & ZM_FLAG_IS_DIR)
          		type = DT_DIR;

          	if (kern_path(rule->virtual_path, LOOKUP_FOLLOW, &path) == 0) {
          		path_put(&path);
          	} else {
          		zeromount_auto_inject_parent(rule->virtual_path, type, 0);
          		WRITE_ONCE(rule->is_new, true);
          	}
          	zeromount_flush_dcache(rule->virtual_path);
          	return 0;
          }

          static void zeromount_cleanup_parent_dir(char *v_path)
          {
          	char *end = v_path + strlen(v_path);
          	char *last_slash, *p;
          	struct zeromount_dir_node *dir_node;
          	struct zeromount_child_name *child, *tmp_child;
          	u32 hash;
          	bool pruned;

          	do {
          		pruned = false;
          		last_slash = strrchr(v_path, '/');
          		if (!last_slash || last_slash == v_path)
          			break;

          		*last_slash = '\0';
          		hash = full_name_hash(NULL, v_path, strlen(v_path));

          		hash_for_each_possible(zeromount_dirs_ht, dir_node,
          				       node, hash) {
          			if (strcmp(dir_node->dir_path, v_path) != 0)
          				continue;

          			list_for_each_entry_safe(child, tmp_child,
          						 &dir_node->children_names,
          						 list) {
          				if (strcmp(child->name, last_slash + 1) == 0) {
          					list_del_rcu(&child->list);
          					call_rcu(&child->rcu,
          						 zeromount_free_child_rcu);
          					break;
          				}
          			}

          			if (list_empty(&dir_node->children_names)) {
          				hash_del_rcu(&dir_node->node);
          				atomic_dec(&zeromount_dirs_count);
          				call_rcu(&dir_node->rcu,
          					 zeromount_free_dir_node_rcu);
          				pruned = true;
          			}
          			break;
          		}
          	} while (pruned);

          	for (p = v_path; p < end; p++)
          		if (*p == '\0')
          			*p = '/';
          }

          static int zeromount_ioctl_del_rule(unsigned long arg)
          {
          	struct zeromount_ioctl_data data;
          	struct zeromount_rule *rule = NULL;
          	struct hlist_node *tmp;
          	char *v_path_raw, *v_path;
          	int bkt;
          	bool found = false;

          	if (copy_from_user(&data, (void __user *)arg, sizeof(data)))
          		return -EFAULT;

          	v_path_raw = strndup_user(data.virtual_path, PATH_MAX);
          	if (IS_ERR(v_path_raw))
          		return PTR_ERR(v_path_raw);

          	v_path = zeromount_normalize_path(v_path_raw);
          	kfree(v_path_raw);
          	if (!v_path)
          		return -ENOMEM;

          	spin_lock(&zeromount_lock);
          	hash_for_each_safe(zeromount_rules_ht, bkt, tmp, rule, node) {
          		if (strcmp(rule->virtual_path, v_path) == 0) {
          			hash_del_rcu(&rule->node);
          			if (rule->real_ino != 0)
          				hash_del_rcu(&rule->ino_node);
          			list_del(&rule->list);
          			found = true;
          			break;
          		}
          	}
          	if (found)
          		zeromount_cleanup_parent_dir(v_path);
          	if (found)
          		atomic_dec(&zeromount_rule_count);
          	zm_bloom_rebuild();
          	spin_unlock(&zeromount_lock);

          	if (found && rule)
          		call_rcu(&rule->rcu, zeromount_free_rule_rcu);

          	kfree(v_path);
          	return found ? 0 : -ENOENT;
          }

          static int zeromount_ioctl_clear_rules(void)
          {
          	struct zeromount_rule *rule;
          	struct zeromount_uid_node *uid_node;
          	struct hlist_node *tmp;
          	int bkt;

          	spin_lock(&zeromount_lock);

          	hash_for_each_safe(zeromount_rules_ht, bkt, tmp, rule, node) {
          		hash_del_rcu(&rule->node);
          		if (rule->real_ino != 0)
          			hash_del_rcu(&rule->ino_node);
          		list_del(&rule->list);
          		call_rcu(&rule->rcu, zeromount_free_rule_rcu);
          	}

          	hash_for_each_safe(zeromount_uid_ht, bkt, tmp, uid_node, node) {
          		hash_del_rcu(&uid_node->node);
          		kfree_rcu(uid_node, rcu);
          	}

          	{
          		struct zeromount_dir_node *dir_node;
          		struct zeromount_child_name *child, *tmp_child;
          		int bkt2;
          		struct hlist_node *tmp2;

          		hash_for_each_safe(zeromount_dirs_ht, bkt2, tmp2,
          				   dir_node, node) {
          			hash_del_rcu(&dir_node->node);
          			atomic_dec(&zeromount_dirs_count);
          			list_for_each_entry_safe(child, tmp_child,
          						 &dir_node->children_names,
          						 list) {
          				list_del_rcu(&child->list);
          				call_rcu(&child->rcu,
          					 zeromount_free_child_rcu);
          			}
          			call_rcu(&dir_node->rcu,
          				 zeromount_free_dir_node_rcu);
          		}
          	}

          	atomic_set(&zeromount_rule_count, 0);
          	bitmap_zero(zm_bloom, ZM_BLOOM_BITS);
          	spin_unlock(&zeromount_lock);
          	return 0;
          }

          static int zeromount_ioctl_list_rules(unsigned long arg)
          {
          	struct zeromount_rule *rule;
          	char *kbuf;
          	int ret = 0;
          	size_t len = 0;
          	size_t remaining;
          	char __user *ubuf = (char __user *)arg;

          	kbuf = vmalloc(MAX_LIST_BUFFER_SIZE);
          	if (!kbuf)
          		return -ENOMEM;

          	memset(kbuf, 0, MAX_LIST_BUFFER_SIZE);
          	spin_lock(&zeromount_lock);

          	list_for_each_entry(rule, &zeromount_rules_list, list) {
          		remaining = MAX_LIST_BUFFER_SIZE - len;
          		if (remaining <= 1) {
          			ret = -ENOBUFS;
          			break;
          		}
          		len += scnprintf(kbuf + len, remaining, "%s->%s\n",
          				 rule->real_path, rule->virtual_path);
          	}

          	spin_unlock(&zeromount_lock);

          	if (ret == -ENOBUFS) {
          		vfree(kbuf);
          		return ret;
          	}

          	if (copy_to_user(ubuf, kbuf, len))
          		ret = -EFAULT;
          	else
          		ret = len;

          	vfree(kbuf);
          	return ret;
          }

          static int zeromount_ioctl_add_uid(unsigned long arg)
          {
          	unsigned int uid;
          	struct zeromount_uid_node *entry;

          	if (copy_from_user(&uid, (void __user *)arg, sizeof(uid)))
          		return -EFAULT;
          	if (zeromount_is_uid_blocked(uid))
          		return -EEXIST;

          	entry = kzalloc(sizeof(*entry), GFP_KERNEL);
          	if (!entry)
          		return -ENOMEM;

          	entry->uid = uid;

          	spin_lock(&zeromount_lock);
          	hash_add_rcu(zeromount_uid_ht, &entry->node, uid);
          	spin_unlock(&zeromount_lock);

          	return 0;
          }

          static int zeromount_ioctl_del_uid(unsigned long arg)
          {
          	unsigned int uid;
          	struct zeromount_uid_node *entry;
          	struct hlist_node *tmp;
          	int bkt;
          	bool found = false;

          	if (copy_from_user(&uid, (void __user *)arg, sizeof(uid)))
          		return -EFAULT;

          	spin_lock(&zeromount_lock);
          	hash_for_each_safe(zeromount_uid_ht, bkt, tmp, entry, node) {
          		if (entry->uid == uid) {
          			hash_del_rcu(&entry->node);
          			found = true;
          			break;
          		}
          	}
          	spin_unlock(&zeromount_lock);

          	if (found && entry)
          		kfree_rcu(entry, rcu);

          	return found ? 0 : -ENOENT;
          }

          static int zeromount_ioctl_enable(void)
          {
          	atomic_set(&zeromount_enabled, 1);
          	return 0;
          }

          static int zeromount_ioctl_disable(void)
          {
          	atomic_set(&zeromount_enabled, 0);
          	return 0;
          }

          static long zeromount_ioctl(struct file *filp, unsigned int cmd,
          			    unsigned long arg)
          {
          	if (_IOC_TYPE(cmd) != ZEROMOUNT_MAGIC_CODE)
          		return -ENOTTY;

          	if (cmd != ZEROMOUNT_IOC_GET_VERSION) {
          		if (!capable(CAP_SYS_ADMIN))
          			return -EPERM;
          	}

          	switch (cmd) {
          	case ZEROMOUNT_IOC_GET_VERSION:	return ZEROMOUNT_VERSION;
          	case ZEROMOUNT_IOC_ADD_RULE:	return zeromount_ioctl_add_rule(arg);
          	case ZEROMOUNT_IOC_DEL_RULE:	return zeromount_ioctl_del_rule(arg);
          	case ZEROMOUNT_IOC_CLEAR_ALL:	return zeromount_ioctl_clear_rules();
          	case ZEROMOUNT_IOC_ADD_UID:	return zeromount_ioctl_add_uid(arg);
          	case ZEROMOUNT_IOC_DEL_UID:	return zeromount_ioctl_del_uid(arg);
          	case ZEROMOUNT_IOC_GET_LIST:	return zeromount_ioctl_list_rules(arg);
          	case ZEROMOUNT_IOC_ENABLE:	return zeromount_ioctl_enable();
          	case ZEROMOUNT_IOC_DISABLE:	return zeromount_ioctl_disable();
          	case ZEROMOUNT_IOC_REFRESH:
          		zeromount_force_refresh_all();
          		return 0;
          	case ZEROMOUNT_IOC_GET_STATUS:	return atomic_read(&zeromount_enabled);
          	default:			return -EINVAL;
          	}
          }

          static struct kobject *zeromount_kobj;

          static ssize_t debug_show(struct kobject *kobj, struct kobj_attribute *attr,
          			  char *buf)
          {
          	return sprintf(buf, "%d\n", zeromount_debug_level);
          }

          static ssize_t debug_store(struct kobject *kobj, struct kobj_attribute *attr,
          			   const char *buf, size_t count)
          {
          	int val;

          	if (kstrtoint(buf, 10, &val) < 0)
          		return -EINVAL;
          	if (val < 0 || val > 2)
          		return -EINVAL;
          	zeromount_debug_level = val;
          	return count;
          }

          static struct kobj_attribute debug_attr =
          	__ATTR(debug, 0600, debug_show, debug_store);

          static struct attribute *zeromount_attrs[] = {
          	&debug_attr.attr,
          	NULL,
          };

          static struct attribute_group zeromount_attr_group = {
          	.attrs = zeromount_attrs,
          };

          static int zeromount_dev_open(struct inode *inode, struct file *file)
          {
          	if (!uid_eq(current_euid(), GLOBAL_ROOT_UID))
          		return -EPERM;
          	return 0;
          }

          static const struct file_operations zeromount_fops = {
          	.owner		= THIS_MODULE,
          	.open		= zeromount_dev_open,
          	.unlocked_ioctl	= zeromount_ioctl,
          #ifdef CONFIG_COMPAT
          	.compat_ioctl	= zeromount_ioctl,
          #endif
          };

          static struct miscdevice zeromount_device = {
          	.minor	= MISC_DYNAMIC_MINOR,
          	.name	= "zeromount",
          	.fops	= &zeromount_fops,
          	.mode	= 0600,
          };

          static int zeromount_reboot_notify(struct notifier_block *nb,
          				   unsigned long action, void *data)
          {
          	atomic_set(&zeromount_enabled, 0);
          	synchronize_rcu();
          	return NOTIFY_OK;
          }

          static struct notifier_block zeromount_reboot_nb = {
          	.notifier_call = zeromount_reboot_notify,
          };

          static int __init __used zeromount_init(void)
          {
          	int ret;

          	spin_lock_init(&zeromount_lock);
          	register_reboot_notifier(&zeromount_reboot_nb);

          	ret = misc_register(&zeromount_device);
          	if (ret)
          		return ret;

          	zeromount_kobj = kobject_create_and_add("zeromount", kernel_kobj);
          	if (zeromount_kobj) {
          		ret = sysfs_create_group(zeromount_kobj,
          					&zeromount_attr_group);
          		if (ret)
          			pr_warn("ZeroMount: sysfs group creation failed: %d\n",
          				ret);
          	}

          	ZM_INFO("Loaded (debug=%d)\n", zeromount_debug_level);
          	return 0;
          }
          fs_initcall(zeromount_init);
          EOF_ZM_C

          python3 - << 'EOF_PYTHON_ZM'
          import os, re

          def inject(file_path, search_str, replace_str, count=1):
              if os.path.exists(file_path):
                  with open(file_path, "r") as f: content = f.read()
                  content = content.replace(search_str, replace_str, count)
                  with open(file_path, "w") as f: f.write(content)

          print("Injecting Kconfig and Makefile...")
          inject("fs/Kconfig", "endmenu", "config ZEROMOUNT\n\tbool \"ZeroMount Path Redirection Subsystem\"\n\tdefault y\n\nendmenu")
          with open("fs/Makefile", "a") as f: f.write("\nobj-$(CONFIG_ZEROMOUNT) += zeromount.o\n")

          print("Injecting fs/d_path.c...")
          inject("fs/d_path.c", '#include "mount.h"', '#include "mount.h"\n#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n')
          hk_dpath = """#ifdef CONFIG_ZEROMOUNT
          	if (path->dentry && d_backing_inode(path->dentry)) {
          		char *v_path = zeromount_get_static_vpath(d_backing_inode(path->dentry));
          		if (v_path) {
          			int len = strlen(v_path);
          			if (buflen < len + 1) {
          				kfree(v_path);
          				return ERR_PTR(-ENAMETOOLONG);
          			}
          			*--res = '\\0';
          			res -= len;
          			memcpy(res, v_path, len);
          			kfree(v_path);
          			return res;
          		}
          	}
          #endif\n"""
          inject("fs/d_path.c", 'return path->dentry->d_op->d_dname(path->dentry, buf, buflen);', hk_dpath + '\treturn path->dentry->d_op->d_dname(path->dentry, buf, buflen);')

          print("Injecting fs/namei.c...")
          inject("fs/namei.c", '#include <linux/pagemap.h>', '#include <linux/pagemap.h>\n#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n')
          inject("fs/namei.c", 'result->refcnt = 1;\n\t}', 'result->refcnt = 1;\n\t}\n#ifdef CONFIG_ZEROMOUNT\n\tif (!IS_ERR(result)) result = zeromount_getname_hook(result);\n#endif')
          
          hk_perm = """
          #ifdef CONFIG_ZEROMOUNT
          	if (zeromount_is_injected_file(inode)) {
          		if (mask & MAY_WRITE) return -EACCES;
          		return 0;
          	}
          	if (S_ISDIR(inode->i_mode) && zeromount_is_traversal_allowed(inode, mask)) return 0;
          #endif
          """
          inject("fs/namei.c", 'int generic_permission(struct inode *inode, int mask)\n{\n\tint ret;', 'int generic_permission(struct inode *inode, int mask)\n{\n\tint ret;' + hk_perm)
          inject("fs/namei.c", 'int inode_permission(struct inode *inode, int mask)\n{\n\tint retval;', 'int inode_permission(struct inode *inode, int mask)\n{\n\tint retval;' + hk_perm)

          print("Injecting fs/proc/base.c...")
          inject("fs/proc/base.c", '#include "internal.h"', '#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n#include "internal.h"')
          hk_base = """#ifdef CONFIG_ZEROMOUNT
          	if (!zeromount_should_skip() && path->dentry) {
          		struct inode *inode = d_backing_inode(path->dentry);
          		if (inode) {
          			char *vpath = zeromount_get_static_vpath(inode);
          			if (vpath) {
          				int vlen = strlen(vpath);
          				if (vlen > buflen) vlen = buflen;
          				if (copy_to_user(buffer, vpath, vlen) == 0) {
          					kfree(vpath);
          					free_page((unsigned long)tmp);
          					return vlen;
          				}
          				kfree(vpath);
          			}
          		}
          	}
          #endif\n"""
          inject("fs/proc/base.c", 'if (!tmp)\n\t\treturn -ENOMEM;', 'if (!tmp)\n\t\treturn -ENOMEM;\n' + hk_base)

          print("Injecting fs/proc/task_mmu.c...")
          inject("fs/proc/task_mmu.c", '#include <asm/elf.h>', '#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n#include <asm/elf.h>')
          inject("fs/proc/task_mmu.c", 'pgoff = ((loff_t)vma->vm_pgoff) << PAGE_SHIFT;', 'pgoff = ((loff_t)vma->vm_pgoff) << PAGE_SHIFT;\n#ifdef CONFIG_ZEROMOUNT\n\t\tzeromount_spoof_mmap_metadata(inode, &dev, &ino);\n#endif')

          print("Injecting fs/statfs.c...")
          inject("fs/statfs.c", '#include "internal.h"', '#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n#include "internal.h"')
          inject("fs/statfs.c", 'error = vfs_statfs(&path, st);', 'error = vfs_statfs(&path, st);\n#ifdef CONFIG_ZEROMOUNT\n\t\t(void)zeromount_spoof_statfs(pathname, st);\n#endif')

          print("Injecting fs/xattr.c...")
          inject("fs/xattr.c", '#include <linux/uaccess.h>', '#include <linux/uaccess.h>\n#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n')
          hk_xa = """#ifdef CONFIG_ZEROMOUNT
          	ssize_t zm_ret = zeromount_spoof_xattr(dentry, name, value, size);
          	if (zm_ret != -EOPNOTSUPP) return zm_ret;
          #endif\n"""
          inject("fs/xattr.c", 'return __vfs_getxattr(dentry, dentry->d_inode, name, value, size, 0);', hk_xa + '\treturn __vfs_getxattr(dentry, dentry->d_inode, name, value, size, 0);')

          print("Injecting fs/stat.c...")
          inject("fs/stat.c", '#include <linux/compat.h>', '#include <linux/compat.h>\n#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n')
          stat_hk = """
          #ifdef CONFIG_ZEROMOUNT
          static inline int zeromount_stat_hook(int dfd, const char *kname, struct kstat *stat, unsigned int request_mask, int flags) {
              if (zm_is_recursive() || IS_ERR_OR_NULL(kname)) return -ENOENT;
              if (kname[0] != '/') {
                  char *abs_path = zeromount_build_absolute_path(dfd, kname);
                  if (abs_path) {
                      char *resolved = zeromount_resolve_path(abs_path);
                      if (resolved) {
                          struct path zm_path;
                          int zm_ret;
                          zm_enter();
                          zm_ret = kern_path(resolved, (flags & AT_SYMLINK_NOFOLLOW) ? 0 : LOOKUP_FOLLOW, &zm_path);
                          zm_exit();
                          kfree(resolved);
                          kfree(abs_path);
                          if (zm_ret == 0) {
                              zm_ret = vfs_getattr(&zm_path, stat, request_mask, (flags & AT_SYMLINK_NOFOLLOW) ? AT_SYMLINK_NOFOLLOW : 0);
                              path_put(&zm_path);
                              return zm_ret;
                          }
                      } else {
                          kfree(abs_path);
                      }
                  }
              }
              return -ENOENT;
          }
          #endif
          """
          inject("fs/stat.c", 'int vfs_fstat(int fd, struct kstat *stat)', stat_hk + '\nint vfs_fstat(int fd, struct kstat *stat)')
          vfs_hk = """orig_flow:
          #ifdef CONFIG_ZEROMOUNT
          	if (fname && !IS_ERR(fname)) {
          		int zm_ret = zeromount_stat_hook(dfd, fname->name, stat, request_mask, flags);
          		if (zm_ret != -ENOENT) {
          			putname(fname);
          			return zm_ret;
          		}
          	}
          #endif
          """
          inject("fs/stat.c", 'orig_flow:\n\terror = filename_lookup(dfd, fname, lookup_flags, &path, NULL);', vfs_hk + '\terror = filename_lookup(dfd, fname, lookup_flags, &path, NULL);')

          print("Injecting fs/readdir.c...")
          if os.path.exists("fs/readdir.c"):
              with open("fs/readdir.c", "r") as f: c = f.read()
              c = c.replace('#include <linux/unistd.h>', '#include <linux/unistd.h>\n#ifdef CONFIG_ZEROMOUNT\n#include <linux/zeromount.h>\n#endif\n')
              
              # Safely inject initial_count right before int error; inside getdents/getdents64 callbacks
              target_pattern = r'(struct getdents_callback buf = \{\s*\.ctx\.actor = filldir,\s*\.dirent = dirent\s*\};\s*\n\t)int error;'
              replacement = r'#ifdef CONFIG_ZEROMOUNT\n\tint initial_count = count;\n#endif\n\tint error;'
              c = re.sub(target_pattern, r'\1' + replacement, c)
              
              skip = """\tif (!f.file)
          		return -EBADF;
          #ifdef CONFIG_ZEROMOUNT
          	if (f.file->f_pos >= ZEROMOUNT_MAGIC_POS) {
          		error = 0;
          		goto skip_real_iterate;
          	}
          #endif"""
              c = c.replace('if (!f.file)\n\t\treturn -EBADF;', skip)
              
              inj_norm = """
          #ifdef CONFIG_ZEROMOUNT
          skip_real_iterate:
          	if (error >= 0 && !signal_pending(current)) {
          		zeromount_inject_dents(f.file, (void __user **)&dirent, &count, &f.file->f_pos);
          		if (count != initial_count) error = initial_count - count;
          		goto zm_out;
          	}
          zm_out:
          #endif
          	fdput_pos(f);"""

              inj_64 = """
          #ifdef CONFIG_ZEROMOUNT
          skip_real_iterate:
          	if (error >= 0 && !signal_pending(current)) {
          		zeromount_inject_dents64(f.file, (void __user **)&dirent, &count, &f.file->f_pos);
          		if (count != initial_count) error = initial_count - count;
          		goto zm_out;
          	}
          zm_out:
          #endif
          	fdput_pos(f);"""
              
              parts = c.split('SYSCALL_DEFINE3(getdents64')
              if len(parts) == 2:
                  p0 = parts[0].replace('\tfdput_pos(f);', inj_norm)
                  p1_parts = parts[1].split('\tfdput_pos(f);', 1)
                  p1 = p1_parts[0] + inj_64 + p1_parts[1].replace('\tfdput_pos(f);', inj_norm)
                  c = p0 + 'SYSCALL_DEFINE3(getdents64' + p1
                  
              with open("fs/readdir.c", "w") as f: f.write(c)

          print("✅ ZeroMount Hooks Injected via Python successfully!")
          EOF_PYTHON_ZM

          echo "=== 4. Directly Injecting Spoof Modifications via Python ==="
          python3 - << 'EOF'
          import os
          import re

          # 1. Modify fs/proc/cmdline.c
          cmdline_path = "fs/proc/cmdline.c"
          if os.path.exists(cmdline_path):
              with open(cmdline_path, "r") as f:
                  content = f.read()
              
              helper_code = r"""
          #include <linux/slab.h>
          #include <linux/string.h>
          #include <linux/cred.h>

          static void replace_str(char *buf, size_t max_len, const char *search, const char *replace)
          {
          	char *p = buf;
          	size_t s_len = strlen(search);
          	size_t r_len = strlen(replace);

          	while ((p = strstr(p, search)) != NULL) {
          		size_t cur_len = strlen(buf);
          		size_t tail_len = strlen(p + s_len);

          		if (r_len > s_len && cur_len + (r_len - s_len) >= max_len)
          			break;

          		if (r_len != s_len)
          			memmove(p + r_len, p + s_len, tail_len + 1);

          		memcpy(p, replace, r_len);
          		p += r_len;
          	}
          }
          """
              
              replacement_func = r"""static int cmdline_proc_show(struct seq_file *m, void *v)
          {
          	char *buf;
          	size_t len;

          	if (!saved_command_line) {
          		seq_putc(m, 10);
          		return 0;
          	}

          	if (__kuid_val(current_uid()) >= 10000) {
          		len = strlen(saved_command_line);
          		buf = kzalloc(len + 256, GFP_KERNEL);
          		if (buf) {
          			memcpy(buf, saved_command_line, len + 1);
          			replace_str(buf, len + 256, "androidboot.verifiedbootstate=orange", "androidboot.verifiedbootstate=green");
          			replace_str(buf, len + 256, "androidboot.verifiedbootstate=yellow", "androidboot.verifiedbootstate=green");
          			replace_str(buf, len + 256, "androidboot.verifiedbootstate=red", "androidboot.verifiedbootstate=green");
          			replace_str(buf, len + 256, "androidboot.vbmeta.device_state=unlocked", "androidboot.vbmeta.device_state=locked");
          			replace_str(buf, len + 256, "androidboot.flash.locked=0", "androidboot.flash.locked=1");
          			replace_str(buf, len + 256, "androidboot.selinux=permissive", "androidboot.selinux=enforcing");
          			replace_str(buf, len + 256, "androidboot.buildvariant=userdebug", "androidboot.buildvariant=user");
          			replace_str(buf, len + 256, "androidboot.buildvariant=eng", "androidboot.buildvariant=user");
          			replace_str(buf, len + 256, "buildvariant=userdebug", "buildvariant=user");
          			replace_str(buf, len + 256, "buildvariant=eng", "buildvariant=user");
          			replace_str(buf, len + 256, "androidboot.mode=custom", "androidboot.mode=normal");
          			replace_str(buf, len + 256, "androidboot.mode=recovery", "androidboot.mode=normal");
          			replace_str(buf, len + 256, "androidboot.secureboot=0", "androidboot.secureboot=1");
          			replace_str(buf, len + 256, "androidboot.lockstate=unlocked", "androidboot.lockstate=locked");
          			replace_str(buf, len + 256, "androidboot.warranty_bit=1", "androidboot.warranty_bit=0");
          			replace_str(buf, len + 256, "androidboot.warranty_bits=1", "androidboot.warranty_bits=0");
          			replace_str(buf, len + 256, "androidboot.force_normal_boot=0", "androidboot.force_normal_boot=1");
          			seq_puts(m, buf);
          			kfree(buf);
          			seq_putc(m, 10);
          			return 0;
          		}
          	}

          	seq_puts(m, saved_command_line);
          	seq_putc(m, 10);
          	return 0;
          }"""

              pattern = r'static int cmdline_proc_show\(struct seq_file \*m, void \*v\)\s*\{.*?\n\}'
              if re.search(pattern, content, re.DOTALL):
                  content = helper_code + "\n" + re.sub(pattern, replacement_func, content, flags=re.DOTALL)
                  with open(cmdline_path, "w") as f:
                      f.write(content)
                  print("Successfully spoofed fs/proc/cmdline.c with UID check")
              else:
                  print("Error: Target func not found in cmdline.c via regex")

          # 2. Modify fs/proc/bootconfig.c
          boot_path = "fs/proc/bootconfig.c"
          if os.path.exists(boot_path):
              with open(boot_path, "r") as f:
                  boot_content = f.read()

              boot_helper = r"""
          #include <linux/string.h>
          #include <linux/cred.h>

          static void replace_xbc_str(char *buf, size_t max_len, const char *search, const char *replace)
          {
          	char *p = buf;
          	size_t s_len = strlen(search);
          	size_t r_len = strlen(replace);

          	while ((p = strstr(p, search)) != NULL) {
          		size_t cur_len = strlen(buf);
          		size_t tail_len = strlen(p + s_len);

          		if (r_len > s_len && cur_len + (r_len - s_len) >= max_len)
          			break;

          		if (r_len != s_len)
          			memmove(p + r_len, p + s_len, tail_len + 1);

          		memcpy(p, replace, r_len);
          		p += r_len;
          	}
          }
          """

              boot_replacement = r"""static int boot_config_proc_show(struct seq_file *m, void *v)
          {
          	if (saved_boot_config) {
          		if (__kuid_val(current_uid()) >= 10000) {
          			char *buf;
          			size_t len = strlen(saved_boot_config);

          			buf = kzalloc(len + 256, GFP_KERNEL);
          			if (buf) {
          				memcpy(buf, saved_boot_config, len + 1);
          				replace_xbc_str(buf, len + 256, "androidboot.verifiedbootstate = \"orange\"", "androidboot.verifiedbootstate = \"green\"");
          				replace_xbc_str(buf, len + 256, "androidboot.verifiedbootstate = \"yellow\"", "androidboot.verifiedbootstate = \"green\"");
          				replace_xbc_str(buf, len + 256, "androidboot.verifiedbootstate = \"red\"", "androidboot.verifiedbootstate = \"green\"");
          				replace_xbc_str(buf, len + 256, "androidboot.vbmeta.device_state = \"unlocked\"", "androidboot.vbmeta.device_state = \"locked\"");
          				replace_xbc_str(buf, len + 256, "androidboot.flash.locked = \"0\"", "androidboot.flash.locked = \"1\"");
          				replace_xbc_str(buf, len + 256, "androidboot.selinux = \"permissive\"", "androidboot.selinux = \"enforcing\"");
          				replace_xbc_str(buf, len + 256, "androidboot.buildvariant = \"userdebug\"", "androidboot.buildvariant = \"user\"");
          				replace_xbc_str(buf, len + 256, "androidboot.buildvariant = \"eng\"", "androidboot.buildvariant = \"user\"");
          				replace_xbc_str(buf, len + 256, "buildvariant = \"userdebug\"", "buildvariant = \"user\"");
          				replace_xbc_str(buf, len + 256, "buildvariant = \"eng\"", "buildvariant = \"user\"");
          				replace_xbc_str(buf, len + 256, "androidboot.mode = \"custom\"", "androidboot.mode = \"normal\"");
          				replace_xbc_str(buf, len + 256, "androidboot.mode = \"recovery\"", "androidboot.mode = \"normal\"");
          				replace_xbc_str(buf, len + 256, "androidboot.secureboot = \"0\"", "androidboot.secureboot = \"1\"");
          				replace_xbc_str(buf, len + 256, "androidboot.lockstate = \"unlocked\"", "androidboot.lockstate = \"locked\"");
          				replace_xbc_str(buf, len + 256, "androidboot.warranty_bit = \"1\"", "androidboot.warranty_bit = \"0\"");
          				replace_xbc_str(buf, len + 256, "androidboot.warranty_bits = \"1\"", "androidboot.warranty_bits = \"0\"");
          				replace_xbc_str(buf, len + 256, "androidboot.force_normal_boot = \"0\"", "androidboot.force_normal_boot = \"1\"");
          				seq_puts(m, buf);
          				kfree(buf);
          				return 0;
          			}
          		}
          		seq_puts(m, saved_boot_config);
          	}
          	return 0;
          }"""

              boot_pattern = r'static int boot_config_proc_show\(struct seq_file \*m, void \*v\)\s*\{.*?\n\}'
              if re.search(boot_pattern, boot_content, re.DOTALL):
                  boot_content = boot_helper + "\n" + re.sub(boot_pattern, boot_replacement, boot_content, flags=re.DOTALL)
                  with open(boot_path, "w") as f:
                      f.write(boot_content)
                  print("Successfully spoofed fs/proc/bootconfig.c with UID check")
              else:
                  print("Error: Target func not found in bootconfig.c via regex")

          # 3. Modify fs/proc/array.c
          array_path = "fs/proc/array.c"
          if os.path.exists(array_path):
              with open(array_path, "r") as f:
                  array_content = f.read()
              
              array_content = array_content.replace('#include <linux/fs_struct.h>', '#include <linux/fs_struct.h>\n#include <linux/cred.h>')
              
              tstate_target = 'seq_puts(m, "State:\\t");\n\tseq_puts(m, get_task_state(p));'
              tstate_repl = 'seq_puts(m, "State:\\t");\n\tif (__kuid_val(current_uid()) >= 10000 && (*get_task_state(p) == \'t\' || *get_task_state(p) == \'T\'))\n\t\tseq_puts(m, "S (sleeping)");\n\telse\n\t\tseq_puts(m, get_task_state(p));'
              array_content = array_content.replace(tstate_target, tstate_repl)

              stat_target = 'state = *get_task_state(task);'
              stat_repl = 'state = *get_task_state(task);\n\tif (__kuid_val(current_uid()) >= 10000 && (state == \'t\' || state == \'T\')) {\n\t\tstate = \'S\';\n\t\twchan = 0;\n\t}'
              array_content = array_content.replace(stat_target, stat_repl)

              with open(array_path, "w") as f:
                  f.write(array_content)
              print("Successfully spoofed fs/proc/array.c")

          # 4. Modify security/selinux/ss/services.c
          services_path = "security/selinux/ss/services.c"
          if os.path.exists(services_path):
              with open(services_path, "r") as f:
                  serv_content = f.read()

              engine_marker = '#include "policycap_names.h"'
              engine_code = (
                  '#include "policycap_names.h"\n\n'
                  '/* --- INJECTED KERNEL RULES: RCU SPOOFING ENGINE START --- */\n'
                  '#include <linux/uidgid.h>\n'
                  '#include <linux/cred.h>\n'
                  '#include <linux/compiler.h>\n'
                  '#include <linux/mutex.h>\n\n'
                  'struct selinux_spoof_ids {\n'
                  '\tu16 cap_c, process_c, binder_c;\n'
                  '\tu32 fsck_t, shell_t, su_t, adbd_t, adbroot_t;\n'
                  '\tstruct rcu_head rcu;\n'
                  '};\n\n'
                  'static struct selinux_spoof_ids __rcu *global_spoof;\n'
                  'static DEFINE_MUTEX(spoof_update_mutex);\n\n'
                  'static void update_spoof_ids(struct selinux_policy *policy)\n'
                  '{\n'
                  '\tstruct selinux_spoof_ids *new_ids;\n'
                  '\tstruct selinux_spoof_ids *old_ids;\n'
                  '\tstruct policydb *policydb = &policy->policydb;\n'
                  '\tu32 i;\n\n'
                  '\tnew_ids = kzalloc(sizeof(*new_ids), GFP_ATOMIC);\n'
                  '\tif (!new_ids) return;\n\n'
                  '\tfor (i = 1; i <= policydb->p_classes.nprim; i++) {\n'
                  '\t\tconst char *n = sym_name(policydb, SYM_CLASSES, i - 1);\n'
                  '\t\tif (!n) continue;\n'
                  '\t\tif (!strcmp(n, "capability")) new_ids->cap_c = i;\n'
                  '\t\telse if (!strcmp(n, "process")) new_ids->process_c = i;\n'
                  '\t\telse if (!strcmp(n, "binder")) new_ids->binder_c = i;\n'
                  '\t}\n\n'
                  '\tfor (i = 1; i <= policydb->p_types.nprim; i++) {\n'
                  '\t\tconst char *n = sym_name(policydb, SYM_TYPES, i - 1);\n'
                  '\t\tif (!n) continue;\n'
                  '\t\tif (!strcmp(n, "fsck_untrusted")) new_ids->fsck_t = i;\n'
                  '\t\telse if (!strcmp(n, "shell")) new_ids->shell_t = i;\n'
                  '\t\telse if (!strcmp(n, "su")) new_ids->su_t = i;\n'
                  '\t\telse if (!strcmp(n, "adbd")) new_ids->adbd_t = i;\n'
                  '\t\telse if (!strcmp(n, "adbroot")) new_ids->adbroot_t = i;\n'
                  '\t}\n\n'
                  '\tmutex_lock(&spoof_update_mutex);\n'
                  '\told_ids = rcu_dereference_protected(global_spoof, lockdep_is_held(&spoof_update_mutex));\n'
                  '\trcu_assign_pointer(global_spoof, new_ids);\n'
                  '\tmutex_unlock(&spoof_update_mutex);\n\n'
                  '\tif (old_ids)\n'
                  '\t\tkfree_rcu(old_ids, rcu);\n'
                  '}\n'
                  '/* --- INJECTED KERNEL RULES: RCU SPOOFING ENGINE END --- */'
              )

              if engine_marker in serv_content:
                  serv_content = serv_content.replace(engine_marker, engine_code, 1)

              av_hook = (
                  '\tcontext_struct_compute_av(policydb, scontext, tcontext, tclass, avd,\n'
                  '\t\t\t\t  NULL);\n\n'
                  '\t/* --- INJECTED KERNEL RULES: AVC BYPASS START --- */\n'
                  '\tif (unlikely(avd->allowed) && scontext && tcontext) {\n'
                  '\t\tif (unlikely(__kuid_val(current_uid()) >= 10000)) {\n'
                  '\t\t\tstruct selinux_spoof_ids *s_ids = rcu_dereference(global_spoof);\n'
                  '\t\t\tif (s_ids) {\n'
                  '\t\t\t\tbool spoof = false;\n'
                  '\t\t\t\tif (s_ids->fsck_t && tclass == s_ids->cap_c && scontext->type == s_ids->fsck_t && tcontext->type == s_ids->fsck_t)\n'
                  '\t\t\t\t\tspoof = true;\n'
                  '\t\t\t\telse if (s_ids->su_t && tclass == s_ids->process_c && scontext->type == s_ids->shell_t && tcontext->type == s_ids->su_t)\n'
                  '\t\t\t\t\tspoof = true;\n'
                  '\t\t\t\telse if (s_ids->adbroot_t && tclass == s_ids->binder_c && scontext->type == s_ids->adbd_t && tcontext->type == s_ids->adbroot_t)\n'
                  '\t\t\t\t\tspoof = true;\n\n'
                  '\t\t\t\tif (spoof) {\n'
                  '\t\t\t\t\tavd->allowed = 0;\n'
                  '\t\t\t\t\tavd->auditdeny = 0;\n'
                  '\t\t\t\t\tavd->auditallow = 0;\n'
                  '\t\t\t\t}\n'
                  '\t\t\t}\n'
                  '\t\t}\n'
                  '\t}\n'
                  '\t/* --- INJECTED KERNEL RULES: AVC BYPASS END --- */'
              )

              target_av_call = r'context_struct_compute_av\(policydb,\s*scontext,\s*tcontext,\s*tclass,\s*avd,\s*NULL\);'
              serv_content = re.sub(target_av_call, av_hook, serv_content, count=1)

              commit_marker = 'security_load_policycaps(state, newpolicy);'
              commit_code = 'security_load_policycaps(state, newpolicy);\n\n\t/* --- INJECTED: Refresh Spoof IDs safely during policy reload --- */\n\tupdate_spoof_ids(newpolicy);'
              serv_content = serv_content.replace(commit_marker, commit_code, 1)

              with open(services_path, "w") as f:
                  f.write(serv_content)
              print("Successfully spoofed security/selinux/ss/services.c")
          EOF
          echo "✅ Python regex injection with UID-checks completed successfully!"

          echo "=== 5. Setting up KernelSU-Next (susfs) via pershoot setup.sh ==="
          curl -LSs "https://raw.githubusercontent.com/pershoot/KernelSU-Next/refs/heads/dev-susfs/kernel/setup.sh" | bash -s dev-susfs

          sed -i 's/CONFIG_KSU=m/CONFIG_KSU=y/g' arch/arm64/configs/gki_defconfig || true
          echo "CONFIG_KSU=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_NEXT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_OVERLAY_FS=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KPROBES=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_HAVE_KPROBES=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KPROBE_EVENTS=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_ZEROMOUNT=y" >> arch/arm64/configs/gki_defconfig

          echo "CONFIG_KSU_SUSFS=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT_REDIRECT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSTAT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_SU=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_ENABLE_LOG=n" >> arch/arm64/configs/gki_defconfig
          echo "✅ Applied successfully!"

      - name: Auto-patch 5.10 Compatibility & Standard audit_fsnotify.c
        run: |
          cd work/common
          echo "=== 1. Auto-patching 5.10 Timer API Bugs ==="
          grep -rl "timer_delete_sync" drivers/ fs/ kernel/ net/ include/ 2>/dev/null | xargs sed -i 's/timer_delete_sync/del_timer_sync/g' 2>/dev/null || true

          echo "=== 2. Ensuring clean standard audit_fsnotify.c ==="
          cat << 'EOF' > kernel/audit_fsnotify.c
          // SPDX-License-Identifier: GPL-2.0-or-later
          /* audit_fsnotify.c -- tracking inodes
           *
           * Copyright 2003-2009,2014-2015 Red Hat, Inc.
           * Copyright 2005 Hewlett-Packard Development Company, L.P.
           * Copyright 2005 IBM Corporation
           */

          #include <linux/kernel.h>
          #include <linux/audit.h>
          #include <linux/kthread.h>
          #include <linux/mutex.h>
          #include <linux/fs.h>
          #include <linux/fsnotify_backend.h>
          #include <linux/namei.h>
          #include <linux/netlink.h>
          #include <linux/sched.h>
          #include <linux/slab.h>
          #include <linux/security.h>
          #include "audit.h"

          /*
           * this mark lives on the parent directory of the inode in question.
           * but dev, ino, and path are about the child
           */
          struct audit_fsnotify_mark {
          	dev_t dev;		/* associated superblock device */
          	unsigned long ino;	/* associated inode number */
          	char *path;		/* insertion path */
          	struct fsnotify_mark mark; /* fsnotify mark on the inode */
          	struct audit_krule *rule;
          };

          /* fsnotify handle. */
          static struct fsnotify_group *audit_fsnotify_group;

          /* fsnotify events we care about. */
          #define AUDIT_FS_EVENTS (FS_MOVE | FS_CREATE | FS_DELETE | FS_DELETE_SELF |\
          			 FS_MOVE_SELF)

          static void audit_fsnotify_mark_free(struct audit_fsnotify_mark *audit_mark)
          {
          	kfree(audit_mark->path);
          	kfree(audit_mark);
          }

          static void audit_fsnotify_free_mark(struct fsnotify_mark *mark)
          {
          	struct audit_fsnotify_mark *audit_mark;

          	audit_mark = container_of(mark, struct audit_fsnotify_mark, mark);
          	audit_fsnotify_mark_free(audit_mark);
          }

          char *audit_mark_path(struct audit_fsnotify_mark *mark)
          {
          	return mark->path;
          }

          int audit_mark_compare(struct audit_fsnotify_mark *mark, u64 ino, dev_t dev)
          {
          	if (mark->ino == AUDIT_INO_UNSET)
          		return 0;
          	return (mark->ino == ino) && (mark->dev == dev);
          }

          static void audit_update_mark(struct audit_fsnotify_mark *audit_mark,
          			     const struct inode *inode)
          {
          	audit_mark->dev = inode ? inode->i_sb->s_dev : AUDIT_DEV_UNSET;
          	audit_mark->ino = inode ? inode->i_ino : AUDIT_INO_UNSET;
          }

          struct audit_fsnotify_mark *audit_alloc_mark(struct audit_krule *krule, char *pathname, int len)
          {
          	struct audit_fsnotify_mark *audit_mark;
          	struct path path;
          	struct dentry *dentry;
          	int ret;

          	if (pathname[0] != '/' || pathname[len-1] == '/')
          		return ERR_PTR(-EINVAL);

          	dentry = kern_path_parent(pathname, &path);
          	if (IS_ERR(dentry))
          		return ERR_CAST(dentry); /* returning an error */

          	audit_mark = kzalloc(sizeof(*audit_mark), GFP_KERNEL);
          	if (unlikely(!audit_mark)) {
          		audit_mark = ERR_PTR(-ENOMEM);
          		goto out;
          	}

          	fsnotify_init_mark(&audit_mark->mark, audit_fsnotify_group);
          	audit_mark->mark.mask = AUDIT_FS_EVENTS;
          	audit_mark->path = pathname;
          	audit_update_mark(audit_mark, dentry->d_inode);
          	audit_mark->rule = krule;

          	ret = fsnotify_add_inode_mark(&audit_mark->mark, path.dentry->d_inode, 0);
          	if (ret < 0) {
          		audit_mark->path = NULL;
          		fsnotify_put_mark(&audit_mark->mark);
          		audit_mark = ERR_PTR(ret);
          	}

          out:
          	dput(dentry);
          	path_put(&path);
          	return audit_mark;
          }

          static void audit_mark_log_rule_change(struct audit_fsnotify_mark *audit_mark, char *op)
          {
          	struct audit_buffer *ab;
          	struct audit_krule *rule = audit_mark->rule;

          	if (!audit_enabled)
          		return;
          	ab = audit_log_start(audit_context(), GFP_NOFS, AUDIT_CONFIG_CHANGE);
          	if (unlikely(!ab))
          		return;
          	audit_log_session_info(ab);
          	audit_log_format(ab, " op=%s path=", op);
          	audit_log_untrustedstring(ab, audit_mark->path);
          	audit_log_key(ab, rule->filterkey);
          	audit_log_format(ab, " list=%d res=1", rule->listnr);
          	audit_log_end(ab);
          }

          void audit_remove_mark(struct audit_fsnotify_mark *audit_mark)
          {
          	fsnotify_destroy_mark(&audit_mark->mark, audit_fsnotify_group);
          	fsnotify_put_mark(&audit_mark->mark);
          }

          void audit_remove_mark_rule(struct audit_krule *krule)
          {
          	struct audit_fsnotify_mark *mark = krule->exe;

          	if (mark) {
          		audit_remove_mark(mark);
          		krule->exe = NULL;
          	}
          }

          static void audit_autoremove_mark_rule(struct audit_fsnotify_mark *audit_mark)
          {
          	struct audit_krule *rule = audit_mark->rule;
          	struct audit_entry *entry = container_of(rule, struct audit_entry, rule);

          	audit_mark_log_rule_change(audit_mark, "autoremove_rule");
          	audit_del_rule(entry);
          }

          /* Update mark data in audit rules based on fsnotify events. */
          static int audit_mark_handle_event(struct fsnotify_mark *inode_mark, u32 mask,
          				   struct inode *inode, struct inode *dir,
          				   const struct qstr *dname, u32 cookie)
          {
          	struct audit_fsnotify_mark *audit_mark;

          	audit_mark = container_of(inode_mark, struct audit_fsnotify_mark, mark);

          	if (WARN_ON_ONCE(inode_mark->group != audit_fsnotify_group) ||
          	    WARN_ON_ONCE(!inode))
          		return 0;

          	if (mask & (FS_CREATE|FS_MOVED_TO|FS_DELETE|FS_MOVED_FROM)) {
          		if (audit_compare_dname_path(dname, audit_mark->path, AUDIT_NAME_FULL))
          			return 0;
          		audit_update_mark(audit_mark, inode);
          	} else if (mask & (FS_DELETE_SELF|FS_UNMOUNT|FS_MOVE_SELF)) {
          		audit_autoremove_mark_rule(audit_mark);
          	}

          	return 0;
          }

          static const struct fsnotify_ops audit_mark_fsnotify_ops = {
          	.handle_inode_event = audit_mark_handle_event,
          	.free_mark = audit_fsnotify_free_mark,
          };

          static int __init audit_fsnotify_init(void)
          {
          	audit_fsnotify_group = fsnotify_alloc_group(&audit_mark_fsnotify_ops);
          	if (IS_ERR(audit_fsnotify_group)) {
          		audit_fsnotify_group = NULL;
          		audit_panic("cannot create audit fsnotify group");
          	}
          	return 0;
          }
          device_initcall(audit_fsnotify_init);
          EOF
          echo "✅ All upstream API bugs automatically healed!"

      - name: Build kernel
        run: |
          chmod +x ./build-kernel.sh
          ./build-kernel.sh ${{ inputs.ccache && '--ccache' || '' }}

      - name: Save ccache cache
        if: ${{ inputs.ccache && always() }}
        uses: actions/cache/save@v4
        with:
          path: work/.ccache
          key: cfx-ccache-${{ env.CLANG_ASSET }}-${{ inputs.kernel_repo }}-${{ inputs.kernel_branch }}-${{ github.run_id }}

      - name: Publish release
        if: ${{ inputs.publish_release }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="$RELEASE_TAG"
          if [ -z "$tag" ]; then
            tag="kernel-$(date -u +%Y%m%d-%H%M)"
          fi
          
          cd "$GITHUB_WORKSPACE/work/dist"
          pkg="$GITHUB_WORKSPACE/work/pkg"
          mkdir -p "$pkg"
          assets=()
          for f in Image Image.lz4 Image.gz vmlinux System.map ikconfig .config; do
            [ -f "$f" ] && assets+=("$f")
          done
          tar -I 'zstd -T0' -cf "$pkg/kernel-dist.tar.zst" .
          assets+=("$pkg/kernel-dist.tar.zst")
          
          if gh release view "$tag" >/dev/null 2>&1; then
            gh release upload "$tag" --clobber "${assets[@]}"
          else
            gh release create "$tag" \
              --title "Templar Kernel GKI $tag" \
              --notes "Built with pershoot KernelSU-Next (susfs), MGLRU, ZeroMount & Full Spoofs. $(date -u '+%Y-%m-%d %H:%M UTC')" \
              "${assets[@]}"
          fi
